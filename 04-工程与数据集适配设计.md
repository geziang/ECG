# SD-LFBT 工程与数据集适配设计

> 版本：v1.2（2026-09-15：新增 §3.0 历史资产复用映射；W0-3 补两套划分明细索引、W0-6 补 prepare_cpsc.py 起点）
> 上游文档：《01-实验主线设计.md》（主线与取舍）；《02-模块调研与大实验设计.md》（E008 大实验矩阵）；《03-数据与新轴方向调研(N1-N8).md》（新轴详档）
> 本文件回答：**代码怎么组织、本地数据集怎么统一适配、W0 开工清单与验收标准。**

---

## 一、定版决策记录（2026-09-15，本版设计的输入约束）

| # | 决策 | 依据 |
|---|---|---|
| 1 | **NFH/Chapman 弃用**（私有数据集，不跑） | 用户判定；N1 语料轨改本地多库 |
| 2 | **MDT_DATA 弃用**（2GB 私有格式 Holter，无用数据集） | 用户判定 |
| 3 | **SNPH 不可得**（沿 01 文档既有结论） | D8 鲁棒评估改道：加性噪声 SNR 曲线 + 37 条件电极错位 + 跨库迁移 |
| 4 | N1 语料 = 本地多库 ≈38,700 条（构成见下表），**零下载** | `E:\数据集` 已清点 |
| 5 | 正式训练在**专用服务器**（协议按 RTX 4090 24G / batch=128 设定）；本地笔记本仅开发/单测 | 用户确认 |
| 6 | 代码库以 `E:\新实验\ECG_SSL_LFBT-main` 为工作副本；`E:\LBTF创新\LBTF\ECG_SSL_LFBT-main` 为历史证据链（只读引用 + 拷贝 `utils/`） | 09-15 核验 |
| 7 | 台账 xlsx 缺失 → W0 从 md 重建 | 09-15 核验 |
| 8 | **E006 状态修正**：实为已跑完（24/24，09-08 formal 归档），AR-B3 上弱正/B0 无增益，未过粗筛线、方向未证伪 | 09-15 核阅 `E006_PhysioSpatial_Baseline_Report_zh.md` |
| 9 | **下游划分定版：体系A（`data/ptbxl`）+ manifest 为冻结协议**；体系B（`data/downstream`、`data/pretrain`）弃用——随机划分（fold 1–10 混布所有 split）、1,513 患者跨 split、464 重复文件（177 条 train∩test 污染）、float64 (8,5000) 格式不符 | W0-3 审计（09-15，`runlog/W0/audit_data_report.json`） |

---

## 二、数据集适配规范（所有库统一入仓）

### 2.1 统一中间格式（对齐冻结协议）

每条记录一个 `.npy`：`float32, shape=(8, 2048)`，500Hz、10s、导联序 `[II, III, V1, V2, V3, V4, V5, V6]`、逐导联 z-score。与现有 `data/pt_pretrain`、`data/downstream` 格式完全一致，训练侧零改动。

### 2.2 各库转换要点

| 库 | 原始位置 | 采样率 | 时长 | 转换要点 |
|---|---|---|---|---|
| PTB-XL | `E:\数据集\ptb-xl`（wfdb .hea/.mat，21,837） | 500Hz | 10s | 沿用现有 `preprocess_ptbxl.py`，不动 |
| CPSC 2018+extra | `E:\数据集\classification-...-2020-1.0.2/training/{cpsc_2018,cpsc_2018_extra}` | 500Hz | **6–60s 变长** | 参考源仓库 `prepare_cpsc.py`；下游取中心 10s 窗，预训练取随机 10s 窗（seed 可复现） |
| Georgia | `.../training/georgia` | 1000Hz | 10s | 重采样 500Hz；仅启用第三库时建下游，预训练全量入 |
| PTB | `.../training/ptb` | 1000Hz | 10s | 重采样；仅入预训练 |
| INCART | `.../training/st_petersburg_incart` | 257Hz | 变长 | 重采样；仅入预训练 |
| ~~Challenge2020 ptb-xl 份~~ | `.../training/ptb-xl` | — | — | **禁用**：与下游评估同源，防泄漏 |

导联映射：以上各库均 12 导联标准命名，直接取 I I II III aVR aVL aVF V1–V6 中的 [II,III,V1–V6]；用 wfdb 读原始物理单位（mV）后再 z-score，不吃 adc_gain 差异的亏。

### 2.3 划分纪律（防泄漏红线，写进单测）

1. **PTB-XL**：官方 `strat_fold`（1–8 预训练 / 9 验证 / 10 测试），零改动。
2. **CPSC（既是 N1 预训练语料又是下游库）**：官方无公开 patient 映射，按"每记录一患者"口径（记录名 Axxxx 唯一）做**标签分层记录级哈希划分** 70/10/20（seed=0，登记入 manifest）。**语料轨预训练只允许使用 CPSC 下游 train 子集**；val/test 患者绝不入预训练。
3. **Georgia（若启用第三库）**：同 CPSC 法划分；启用前预训练已用全量的问题 → 启用时下游报告口径单独声明，或 Georgia 只作零样本/线性探测评估库（倾向后者，干净且免重跑）。
4. **PTB / INCART**：无本地下游，全量入预训练。
5. **泄漏单测**：`test_no_leakage`——manifest 中 `pretrain` 角色样本与任何 `downstream_val/test` 角色样本的 `(source_db, record_id)` 交集必须为空。

### 2.4 manifest v2（多库索引，N3/审计共用）

```json
{
  "record_id": "cpsc_2018/A0001",
  "source_db": "cpsc_2018",
  "patient_id": "A0001",
  "orig_fs": 500,
  "window_policy": "center10s | random10s(seed)",
  "split_role": "pretrain | downstream_train | downstream_val | downstream_test",
  "ptbxl_fold": null,
  "labels_snomed": [...],
  "labels_superclass": [...]
}
```

PTB-XL 的 `patient_id` 来自 `ptbxl_database.csv`（N3 患者级正对采样依赖此列）；其余库按记录名口径。manifest 是唯一事实源，采样器/审计/RunLog 都从它读。

---

## 三、代码组织（开关矩阵，继承 E001 工程范式）

```text
ECG_SSL_LFBT-main/
├─ run_pt.py / run_lp.py / run_ft.py      # B0 参照入口，保持只读（不动损失与模型定义）
├─ utils/                                  # 从源仓库拷入（checkpoint/metrics/multilabel）
├─ sd/                                     # SD-LFBT 新增包，按模块分文件
│  ├─ losses/vicreg.py                     # D9：inv/var-hinge/cov 三项 + BN→LN/白化开关
│  ├─ losses/target_matrix.py              # D1L：结构化 T^ij（Einthoven+胸导邻接）+ NEG 随机图
│  ├─ models/decouple.py                   # D3：z=[c;s] 分头 + 约束重路由 + 能量比监控
│  ├─ models/mamba.py                      # D4：CNN+Mamba 主干 + G-D4 参数对齐对照构造器
│  ├─ models/recon.py                      # D7：时间掩码重建支路（轻量 decoder）
│  ├─ aug/physics.py                       # D8 物理噪声库 + N7 LeadDropout
│  ├─ samplers/patient_pairs.py            # N3 患者级附加正对（读 manifest patient_id）
│  ├─ samplers/multilib.py                 # N1 多源混合采样（按 manifest source_db 控比例）
│  ├─ optim/ema.py                         # N4 权重 EMA（默认开，报 with/without 两行）
│  └─ train.py                             # 统一入口：读 config → 组装开关 → 训练 → 自动记账
├─ prepare_multilib.py                     # 二节管线：四库入库 + manifest v2 + 抽查可视化
├─ audit_data.py                           # 数据审计：fold 计数、目录甄别、SHA256、泄漏检查
├─ configs/*.yaml                          # 每个 E008-G1-* 配置一份（唯一变量的载体）
├─ tests/                                  # 单测：开关全关==B0 逐位一致 / 参数账 / 无泄漏
└─ runlog/                                 # 每次运行自动追加：参数量/FLOPs/显存/指标
```

### 3.0 复用优先（历史资产映射，明细见《05》§六）

1. `sd/train.py` 的编排**直接采用 E006/E007 实战验证过的四阶段管线范式**（`run_e006_pipeline.py` / `run_e001_pipeline.py`：preflight→smoke→screen→formal，`--resume` 断点续跑），不重新发明。
2. P1/S1 现成实现在 E006 工程 `models/physiospatial.py`（含双基线配置机制）；D8 附检"+P1/S1"直接加载。
3. 单测范式沿 `tests/test_e006_physiospatial.py`（模块/梯度/checkpoint 烟雾）+ `test_eta_zero` 逐位一致范式。
4. 鲁棒评估复用 `evaluate_robustness.py` + E007 T6 37 条件管线；效率账复用 `benchmark_efficiency.py`。
5. 统计口径（配对差/穷举 bootstrap/Wilcoxon）沿 E007 `recompute_3seed_output.txt` 的输出格式。

### 3.1 关键工程约束（红线落地）

1. **唯一变量**：每个 config 只开一个模块开关；`开关全关` 时 `sd.train` 的前向/损失与 `run_pt.py` 在同 seed、CPU、固定输入下**逐位一致**（沿用 `test_eta_zero_is_bit_identical_to_baseline` 范式）。
2. **参数账硬断言**：每配置自动记录参数量/FLOPs/显存峰值；D4 必须带"同 Mamba 主干 + 原 LFBT 目标、参数量 ±5%"对照（G-D4），无对照批次作废。
3. **归因自证**：D1L 必带 NEG 随机图负对照；D3 每 20ep 记录 c/s 能量比。
4. **test 只评一次**：val 选 checkpoint，resume 幂等。
5. **RunLog 自动汇入**：对齐台账 Sheet 6，结果同步回 xlsx。

### 3.2 模块超参起步值（沿 02 文档，不重新发明）

D9 权重网格 {(25,25,1),(25,10,1),(10,25,1)}×{LN, iter-norm}；D3 c:s 维比 {1:1,1:3}、正交权重 1e-2；D7 时间 mask 0.5、η=0.1；D8 噪声先单一后组合、SNR 宽→窄；N7 k∈{1,2}、丢弃概率 {0.3,0.5}；N3 附加对占比 {25%,50%}。

---

## 四、W0 开工清单（顺序执行，每步带验收标准）

| # | 任务 | 验收标准 |
|---|---|---|
| 1 | ✅ 拷 `utils/` 入工作副本（09-15 完成） | `run_pt.py` 等脚本 import 自检通过 |
| 2 | 服务器环境核对 | Python 3.10 + torch 2.0+cu118（离线 wheel 在源仓库）；`pip install mamba-ssm causal-conv1d wfdb pyyaml`；GPU 可见 |
| 3 | ✅ `audit_data.py` 数据审计（09-15 完成，报告 `runlog/W0/audit_data_report.{json,md}`） | ① pt_pretrain=17,418 ✅、SHA256 ✅、npy 格式 ✅；② pretrain(21,837, HR命名) 为未用全集，甄别标记；⑤ **划分定版**：体系A+manifest 与官方 fold/患者隔离口径自洽 → 冻结协议；体系B 弃用（证据见决策 #9），后续在服务器上移除或加 DEPRECATED 标记 |
| 4 | LP 复现校验（服务器） | 用现成 checkpoint 跑 LP，AUPRC 回到 0.7177±0.5pt 内，确认管线与 E001 行为一致 |
| 5 | 台账 xlsx 重建 | D1–D10 + N1–N8 + RunLog 三 sheet，内容与本套 md 一致 |
| 6 | `prepare_multilib.py` 四库入库（起点：源仓库 `prepare_cpsc.py` + `data/cpsc2018/` 既有划分表，按 §2.3 患者口径复核） | manifest v2 齐全；每库抽查 8 条可视化（波形/导联序/z-score）；泄漏单测通过 |
| 7 | smoke 单测 | 开关全关==B0 逐位一致（1 batch CPU）；参数账断言生效 |
| 8 | G1-N1 语料轨启动 | 多库混合 + 原版 LFBT 目标预训练排队，判死活（PTB-XL ≥ B0 且跨库改善） |

W0 全绿后进入 W1–W2：G1-D9 粗筛 + batch 曲线（128/512），N4 附带验证。

---

## 五、风险与待定

| 事项 | 状态 |
|---|---|
| CPSC"每记录一患者"是近似口径（官方未给映射） | 已在 manifest 标注；如后续找到官方映射可无成本替换（重跑划分单测即可） |
| Georgia 第三库启用条件 | 仅当双库不伤害验证后仍需更强泛化证据时启用；倾向作零样本/LP 评估库，避免预训练全量使用带来的口径纠缠 |
| N3×D3、N7×D3 联动设计（03 文档 4.3 两处 ⚠️） | 在对应模块进入确认阶段前补实现说明，不阻塞当前启动 |
| 变长记录窗策略的可复现性 | 预训练随机窗以 `(record_id, epoch, view_idx)` 派生 seed，保证 resume 幂等 |
