# SD-LFBT 模块调研与大实验设计（详细方案）

> 版本：v1.4（2026-09-15：D4/D7/D8/D1L 补入 E006 设计库复用映射；详见《05-历史实验与代码资产索引.md》）
> 上游文档：《01-实验主线设计.md》（方向与取舍）；《LFBT实验方向清单.xlsx》（模块台账 D1–D10）；《03-数据与新轴方向调研(N1-N8).md》（新轴详档）
> 本文件回答两个问题：**基于主线我们调研了什么模块、证据如何；准备用哪些模块设计大实验、怎么跑。**

---

## 第一部分 基线与历史实验盘点

### 1.1 基线 LFBT 的四个结构性短板

LFBT（Liu et al., *Information Fusion* 2025）以 intra-lead + inter-lead 双 Barlow Twins 损失做多导联 ECG 自监督预训练。经 E001 复现与 E002–E007 试错，确认四个瓶颈：

| # | 短板 | 表现 | 对应表格模块 |
|---|---|---|---|
| B-1 | inter-loss 把所有跨导联相关**无差别压 0** | 解剖相关导联（相邻胸导、肢体导）的共有心电源被当冗余删除 | D1 / D2 / D3 |
| B-2 | BT 目标 + BN **强依赖大 batch** | 论文 Table 6 显示 batch 2048 最优；我们算力下只能跑 batch 128，目标函数处于最弱区间 | D9 |
| B-3 | VGG 式 1D-CNN **局部感受野** | 房颤、传导阻滞等节律病需要跨心动周期的长程信息 | D4 / D5 |
| B-4 | 增强仅 RRC-TO | 真实噪声/电极错位/跨设备场景退化明显 | D8 |

### 1.2 历史实验结论（证据链）

**冻结协议**（E001 确立，全程续用）：PTB-XL，8 导联 [II, III, V1–V6]，500Hz/10s→2048 点，逐导联 z-score；预训练 200ep、batch 128、Adam 1e-3、γ=0.8、λ=0.0051、projector 2048；下游 LP/FT10/FT100；5-seed 配对检验。

| 实验 | 方向 | 关键结果 | 判定 |
|---|---|---|---|
| E001 (08-08) | LFBT 基线复现 | B0：LP 0.7137±0.0036 / FT100 0.7200 / FT10 0.6487（3 seed）；与论文锚点（LP 0.6923 / FT 0.7257）偏差 <1pt | ✅ 基线成立 |
| E002 (08-11) | AR-LFBT 模块筛选（B1–B4：Attention/ECA/Fusion-BT/Mean 等） | 无稳定提升，Mean 基本持平（AR-B3 留作对照基线），LeadMask 待复验 | ❌ 注意力族无效 |
| E003 (08-13) | 六类跨领域模块单消融 + 自动复合（GRN/Mixer/JEPA 等） | GRN 仅低成本趋势；Mixer 明显退化；JEPA 缓解但未回到 B0；复合失败 | ❌ 复合路线无效 |
| E004 (08-17) | GRN-LFBT 专项验证 | FT-10 五 seed 仅小幅趋势，机制归因不足，终止（后改写为 LRTC-Net 应用论文单独成稿） | ❌ 方法学路线终止 |
| E005 (08-19) | 可靠性感知跨导联掩码蒸馏（师生蒸馏族） | R06 vs R01：clean −0.023、双缺失 −0.009 | ❌ 证伪 |
| E006 (08-24 设计 / 09-08 formal) | 结构化生理-空间先验（P1 波形-STFT 谱对齐 + S1 分组 BT 对齐；2 基线 × 4 消融 × 3 seed，24/24 完成） | **增益基线依赖**：AR-B3 上 full LP +0.0040±0.0019（3/3）、AUPRC +0.0104；B0 上 full LP −0.0016（0/3）无增益；量级 <1pt 粗筛线，n=3 无显著性检验 | ⚠️ 弱正未过线，方向未证伪 |
| E007 (09-08) | LRTC-Net(LGA) 证据链 M0–M3（LGA=导联解剖分组对齐辅助损失，零参数） | M1−M0 ft100 **−0.0089**（95%CI [−0.0162, −0.0020]，显著为负）；M3−M2（TRC+LGA）+0.0025 不显著、ft10 −0.0048 显著为负；T6 导联缺失下 M3 仅 +0.003 边际 | ❌ LGA 无增益 |

### 1.3 五条教训（直接约束大实验设计）

1. **注意力/加权类关系模块已死**（E002）→ 导联轴必须改"目标矩阵/解耦结构"，不再做重加权。这也直接否决了 D1 的重 GAT 版。
2. **轻量辅助项淹没在噪声里**（E003/E004/E007）→ 有效增益门槛应按实测 seed 方差设定（见 3.4），粗筛阈值 Δ≥1pt。
3. **"拉近同组导联"是错误方向**（E007 LGA 显著为负）→ D1 的框架必须是"结构化**去相关目标**"（目标矩阵非 0 非全 1），而非"组内对齐"。
4. **师生蒸馏族已被证伪一次**（E005 clean −0.023）→ D10（EMA 自蒸馏+原型）放弃，D7 只做**重建式** MIM，不做蒸馏式。
5. **时间轴要做就换真主干**（E004 GRN 失败）→ D4 直接上 Mamba 级全局建模，且必须带参数量对齐对照，否则增益归因到 backbone 而非机制。
6. **先验类辅助项的增益是基座依赖的**（E006）：同一 P1/S1 先验在鲁棒融合基座（AR-B3=mean+LeadMask）上全协议弱正、在 B0 上无效 → 评估"方向是否可行"必须区分"量级不足"与"方向证伪"，E006 属前者；且 S1（对齐式）弱正 vs LGA（拉近式）显著负，进一步否定"拉近"框架、支持"对齐/去相关目标"框架。

---

## 第二部分 模块调研（D1–D10 逐项结论）

> 每项含：假设 → 外部文献证据 → 内部历史证据 → 判定与定位。外部文献清单见附录 A。

### D1 生理拓扑结构化目标（轻量版 D1L 入选，重 GAT 版弃）

- **假设**：inter-loss 目标矩阵 T^ij 从全 0 改为按生理拓扑设定（肢体导按 Einthoven 六轴夹角、相邻胸导按空间邻接给非零目标），保留应保留的跨导联相关。
- **外部证据**：Einthoven 六轴系统与胸导空间排布是心电标准知识；结构化目标在图 SSL（如 BGRL、Graph-BT 类）中是常见稳定做法。2025–2026 已有 ECG 基础模型（NEJM AI、Nature Comm. ECG-LFM）走 Transformer+大规模数据路线，**没有人做"BT 目标矩阵的生理结构化"**，新颖性空位存在。
- **内部证据（关键警示）**：E007 的 LGA 正是"导联关系先验"家族，结果显著为负——但 LGA 是**组内拉近**（invariance 方向），D1L 是**结构化去相关目标**（correlation 目标值方向），机制相反。E002 已证注意力族无效 → GAT 版（在 8 节点图上做消息传递）双重风险，弃。
- **判定**：✅ 有条件入选（P0）。只做轻量固定权重版；**必须带 NEG 负对照**（随机图/打乱先验）；与 D3 同插槽竞争（E006 设计库 S04 可学习邻接 = 设计中的 S2，不单独成线，仅作 D1L 的 ablation 参考，见《05》§5.1）。E006-S1 已实跑为弱正（非负），"为负判 D3"触发器未触发；S1（对齐式）弱正 vs LGA（拉近式）显著负的反差，支持本方向坚持"对齐/去相关目标"框架。

### D2 偏相关/精度矩阵（降级，并入 D3）

- **假设**：边际相关含共享心电源的共同因，扣除后在偏相关层去冗余更精准。
- **外部证据**：精度矩阵/图套索在统计学习成熟，但高维小样本下方差大，shrinkage 敏感是公认问题。
- **内部证据**：batch 128 下每步可用的样本统计量有限，协方差求逆噪声直接进梯度。
- **判定**：⚠️ 不单独成线。保留"残差化共享源"轻变体作为 **D3 的 ablation**（先回归掉共享源再分解，验证 D3 的 c 头是否已捕获同一东西）。

### D3 共源-特异解耦（入选，方法学主创新）

- **假设**：LFBT 未区分"导联共有心电源 c"与"导联特异视角 s^i"，把共性当冗余抹除。显式分解 z^i=[c; s^i]：c 跨导联/跨视图一致（intra 约束作用于 c），{s^i} 之间去相关（inter 只作用于 s），c⊥s 正交防串味。
- **外部证据**：多视图 common/specific 分解是成熟方法族（DCCAE、MISA 等一脉），塌缩问题有标准监控与缓解手段（两路能量监控、正交约束、分头初始化）；ECG 基础模型领域尚无"导联级共源-特异分解 + BT 目标"的工作。
- **内部证据**：E007 T6 显示导联缺失场景有结构性需求（双缺失掉幅 3.4pt）；D3 的 c 头天然支撑导联置换/缺失鲁棒（E5-1 的机制卖点）。
- **判定**：✅ 入选（P0，主创新）。监控项：c/s 能量比、导联置换下 c 的稳定性（E3-3 DCI）。
- **风险与对策**：分解塌缩 → 正交权重 1e-2 起步、每 20ep 记录两路能量；若 c 吞掉全部信息则退回 D1L。

### D4 Mamba 全局时序主干（入选，带硬前提）

- **假设**：局部 CNN 看不到跨周期节律；CNN 保形态 + Mamba（SSM）建长程，L_temp 约束同一 ECG 相邻时段/相邻 beat 全局表征一致。
- **外部证据**：ECG-Mamba（2025）、自适应 SSM 长程建模（Sci. Rep. 2025）、自监督 Mamba ECG 预训练（2025）已证明 SSM 在 ECG 上技术可行且线性成本；但它们都是"整模型替换"，**未与导联结构化解耦结合**——新颖性落点只能在结合处，这点写作时要明确。
- **内部证据**：E004 证明轻量 GRN 适配器不够（时间轴瓶颈是真的，小修没用）；要么 Mamba 级全局建模，要么不做。（注：E006 设计库的 S07 是**导联序列** Mamba，属空间轴且被 E003 导联混合族负结果覆盖，不等于也不替代本方向，不采纳——见《05》§5.2）
- **判定**：✅ 入选（P0，有条件）。**硬前提 G-D4：必须加"同 Mamba 主干、无 LFBT 改动、参数量对齐"的对照**，把 backbone 增益与结构化解耦增益分开归因，否则整条时间轴结论无效。
- **风险与对策**：显存/算力上升（E7 单独披露效率账）；R 峰检测不稳则先关周期编码。

### D5 时-频双分支（第二梯队，视算力）

- **假设**：只在时域编码丢失频谱形态；AF f 波（4–7Hz）、ST 改变在频域更可分。
- **外部证据**：时频双分支在 ECG 分类中常见且有效；但多作为应用性组合，方法学新颖性一般。
- **内部证据**：E006 的 P1（波形-log-STFT 特征一致性）**已实跑**：AR-B3 上 physiology 单开 LP +0.0026 / FT100 +0.0027（3/3），B0 上无增益——前哨信号弱正未过粗筛线；D5 维持第二梯队，若启动粗筛需基于"鲁棒基座"假设设计并在 S1（+D8/N7）之后排期。
- **判定**：⚠️ 第二梯队。S3（+D4）完成后再决策；算力翻倍是主要成本。

### D6 多尺度 multi-crop（弃）

- **判定理由**：1–2.5s 短窗与 10s 窗的正样本对**语义不一致**（短窗内无节律信息），是结构性缺陷而非调参问题；与 D4 功能重叠；E003 复合实验已示同类"视图扩展"（JEPA 缓解但未回 B0）不乐观。5 seed 下大概率过不了显著性。

### D7 掩码重建 MIM（入选，冲点，仅重建式）

- **假设**：BT 纯判别式，不学波形内部结构；对比+生成互补，通常稳涨。
- **外部证据**：MaeFE（2023，100+ 引用）、MAE-ECG（Sawano 2024）充分验证 ECG 上 MIM 有效；**注意 CoRe-ECG（2026）已做 12 导联对比+重建协同且引用 LFBT** → D7 只能定位为工程增益/冲点模块，不能当方法学主创新，写作时需与 CoRe-ECG 划清边界（我们的重建支路服务于导联解耦主线，非独立贡献）。
- **内部证据**：E005 证伪的是**蒸馏式**掩码（师生框架），重建式未被试过，机制不同，不受该负结果约束。**现成设计种子**：E006 设计库 P03/P10/P11 已给出避开师生蒸馏的轻量掩码方案（P03=片段/频谱遮挡+同模型一致性、P10=导联×时间 patch 掩码、P11=MAE/1D-ViT 独立强基线退路），见《05》§5.1。
- **判定**：✅ 入选（P0 冲点）。时间 mask、比例 0.5 起步；η 从 0.1 起步（E007 T5 显示 η=0.1 档最优的历史偏好，重建损失量级需与 L_BT 核对）。

### D8 物理安全增强 + 频谱一致（入选，防御）

- **假设**：RRC-TO 偏弱；真实噪声/电极错位/跨设备是落地瓶颈。
- **外部证据**：物理噪声库（基线漂移 0.15–0.5Hz、工频 50/60Hz、肌电、幅度缩放、时间扭曲）是标准工程，低风险；自监督 Mamba ECG（2025）显示预训练+鲁棒评估组合有效。
- **内部证据**：E007 T6 的 37 条件导联缺失评估管线可直接复用为 E5 系列底座；E006 设计库 R02 提供噪声/缺失一致性损失形式参照（注意 R02 为 FT 阶段设计，N7 取其预训练增强化改造，见《05》§5.1）。
- **判定**：✅ 入选（P1 防御）。判据宽松：干净库持平（回落 ≤0.3pt）+ 噪声/缺失场景显著优于基线即算成功。

### D9 VICReg 式方差-协方差解耦 + 白化（入选，底座，全项目最先做）

- **假设**：BT 把"保方差（对角→1）"与"去相关（非对角→0）"耦合在一个相关矩阵上且强依赖 BN 沿 batch 统计；拆成 invariance + variance hinge + covariance 三项并用 LN/白化替代 BN 后，小 batch 下稳定。
- **外部证据**：VICReg（Bardes et al. 2022）证明三项解耦对 batch 大小鲁棒，这一结论在多个模态复现。
- **内部证据（本方向的最大机会）**：**我们冻结协议 batch=128，恰在论文 Table 6 曲线的最弱区间**——D9 不是锦上添花，是把我们自己的训练底座从最弱区间拉出来的直接手段。胜条件定义为"batch 128 ≥ 原 BT batch 128 显著、且 batch 曲线更平"，不以"2048 下反超"为目标。
- **判定**：✅ 入选（P0，第一优先）。参考权重 (inv, var, cov)=(25, 25, 1) 起步；`η=0` 与 B0 逐位一致的单测范式（E001 工程）直接沿用。

### D10 EMA 自蒸馏 + 原型（弃，最多附录）

- **判定理由**：E005 已证伪师生蒸馏族一次（clean −0.023）；DINO 系调参最敏感（momentum/temperature/K/centering 全要扫）；在 D9 底座上边际收益存疑。除非终稿前差最后一口气，不再投入。

### 新轴方向速览（N1–N8，详档见《03-数据与新轴方向调研(N1-N8).md》）

模块轴之外的四条从未动过的轴——数据、采样、优化、鲁棒/元数据。它们与模块轴正交，**E002–E007 的全部负结果不覆盖它们**：

| 编号 | 方向 | 轴 | 核心依据 | 决策 |
|---|---|---|---|---|
| N1 | 多库联合预训练 | 数据 | 语料从 1.7 万条补到 ≈3.87 万条（本地多库，零下载），补规模短板 | ✅ 底座级（语料轨并行） |
| N2 | 合成数据预训练/增广 | 数据 | SimECG：合成预训练匹敌真实（26 任务 +5.49% F1） | ✅ 冲点/机理故事 |
| N3 | 患者级正样本对 | 采样 | PTB-XL 21,837 条/18,885 人，复诊记录是天然正对 | ✅ 低成本增广 |
| N4 | 权重 EMA / model soup | 优化 | 不改损失不加分支，与 E005 证伪的蒸馏机制不同 | ✅ 免费附件，默认开 |
| N7 | 导联丢弃一致性增强 | 鲁棒 | E002 LeadMask 是唯一未关闭线索；纯增强无蒸馏 | ✅ 并入 D8 |
| N5 | SAM/F-SAM 优化器 | 优化 | 证据强但训练×2，且 2026 有"虚报"批评 | ⚠️ 第三梯队 |
| N6 | 多层辅助 BT 损失 | 表征深度 | projector 中间层正则，LP 受益 | ⚠️ D9 延伸项 |
| N8 | 元数据辅助头（age/sex） | 元数据 | ECG-FM 有先例，但牺牲纯 SSL 定位 | ⚠️ 附录/防御 |

明确不开：ECG-文本跨模态（无文本数据+算力不够）、backbone scaling study（ECG-LFM 千万级封顶）、聚类/师生蒸馏族（E005 证伪）。

### 调研结论汇总

| 模块 | 决策 | 定位 | 关键依据 |
|---|---|---|---|
| D9 | ✅ 入选 | 底座（最先做） | batch=128 现实约束 + VICReg 外部证据充分 |
| D3 | ✅ 入选 | 方法学主创新 | 解耦框架成熟、ECG 导联级空位、支撑导联缺失鲁棒 |
| D1L | ✅ 有条件入选 | 关系层对照/辅助 | 机制清晰但同族 LGA 负结果警示，靠 NEG 自证 |
| D4 | ✅ 入选（带 G-D4 硬前提） | 时间轴主力 | GRN 失败证明必须换真主干；ECG-Mamba 证明可行 |
| D7 | ✅ 入选 | 冲点 | MIM 外部证据强；须与 CoRe-ECG 划界 |
| D8 | ✅ 入选 | 鲁棒防御 | 低风险高叙事价值；E007 T6 管线复用 |
| D2 | ⚠️ 降级 | D3 的 ablation | 高维小样本估计脆弱 |
| D5 | ⚠️ 第二梯队 | S3 后决策 | 算力翻倍；E006-P1 作前哨 |
| D6 | ❌ 弃 | — | 短窗语义不一致（结构性缺陷） |
| D10 | ❌ 弃 | 至多附录 | E005 证伪同族 + 调参成本高 |
| N1 多库语料 | ✅ 底座级 | 语料轨（见 3.1） | 弥补 1.7 万条 vs 论文 4.4 万条的语料短板 |
| N4 权重 EMA | ✅ 免费附件 | 全配置默认附带 | 不改损失不加分支，与 E005 蒸馏不同机制 |
| N3 患者级正对 | ✅ 增广 | 挂 D9 底座粗筛 | 采样轴零历史 |
| N2 合成数据 | ✅ 冲点/故事 | 与 D7 同期 | SimECG 证据强，可控参数支撑机理图 |
| N7 导联丢弃 | ✅ 并入 D8 | S5 步 | E002 LeadMask 线索未关闭，纯增强无蒸馏 |
| N5 / N6 / N8 | ⚠️ 第三梯队 | 主线定型后按序试 | 各带停止规则 |
| （E002–E005 旧方向） | ❌ 不再投入 | — | 见 1.2 证据链 |

---

## 第三部分 大实验设计（G 系列）

> 编号从 **E008** 起接入既有档案体系；本大实验整体立项为 **E008: SD-LFBT**。

### 3.0 总目标

在冻结协议上，沿 D9 → D3/D1L → D4 → D7 → D8 的顺序累加，产出一个每个模块都有单插证据、负对照、留一消融支撑的 SD-LFBT 主模型，并以多库（PTB-XL / CPSC，Georgia 可选第三库）不伤害约束验证泛化。

### 3.1 协议设计：两阶段扩展

| 阶段 | 数据 | 用途 |
|---|---|---|
| Phase A（主线迭代） | PTB-XL 冻结协议（与 E001–E007 完全可比） | 单模块粗筛与确认，速度快、历史可比 |
| Phase B（泛化验证） | + CPSC（9 类；Georgia 可选第三库） | 进入累加矩阵 S2 起的每个配置都跑多库；执行不伤害约束 |
| Phase 0（语料轨，与 Phase A 并行） | N1：本地多库混合**预训练**语料（PTB-XL folds1-8 + CPSC 10,330 + Georgia 10,344 + PTB 516 + INCART 74 ≈ 38,700 条；NFH/Chapman 已判私有弃用；Challenge2020 的 ptb-xl 份与下游同源禁用） | 多库语料 + 原版 LFBT 目标单独验证；PTB-XL 下游 ≥ B0 且跨库改善 → G2 起切换语料底座 |

说明：涨点空间主要在 PTB-XL（0.69）与 CPSC（0.73）；Georgia 无本地锚点，仅在启用为第三库时作不伤害/迁移监控，不做涨点预期。语料轨判据与并行规则详见《03》4.1：G1 模块粗筛全部留在 PTB-XL 语料（保历史可比），两轨在 G2 累加阶段合流。

### 3.2 两段式筛选（省算力，红线不破）

```text
粗筛（Screen）：1–2 seed，仅 PTB-XL LP/FT10，200ep 或减半
   通过线：Δ AUPRC ≥ +1.0pt（高于 seed 噪声上界，见 3.4）
      ↓
确认（Confirm）：5 seed × 三库 × LP+FT100+FT10，锁定 test 一次性评估
   报告口径：均值±std + 配对 Wilcoxon + 穷举 bootstrap 95%CI
```

粗筛不通过者直接终止并归档（照记 RunLog，不选择性报告）；粗筛通过但确认不过显著性者也终止。这与 E006 的 stop rules 一致。

### 3.3 实验矩阵

#### G1 单模块消融（对齐清单 Sheet 2，编号 E008-G1-*）

| ID | 模块 | 实验组（唯一变量） | 扫描点（粗筛阶段） | 确认判据 |
|---|---|---|---|---|
| G1-D9 | D9 | BT 三项拆分 + BN→LN/白化 | 三项权重 {(25,25,1), (25,10,1), (10,25,1)}×白化{LN, iter-norm} | batch 128 下 Δ>0 且 CI 下界>0；batch 曲线（128/512/2048）更平 |
| G1-D3 | D3 | z=[c;s] 分解 + 约束重路由 | c:s 维比 {1:1, 1:3}；正交权重 {1e-2, 1e-1} | Δ>0 且 CI 下界>0；DCI/置换稳定（E3-3） |
| G1-D1L | D1L | inter 目标矩阵 T^ij 结构化 | T 来源 {Einthoven+邻接, 仅胸导邻接}；w_ij 档位 | Δ>0 且 CI 下界>0；**NEG 随机图不涨** |
| G1-D4 | D4 | CNN+Mamba + L_temp | 全局模块 {Mamba, 空洞卷积对照}；α {1e-3,1e-2}；周期编码开关 | 节律病组 Δ 显著>形态病组；**G-D4 对照下增益仍在** |
| G1-D7 | D7 | + 重建支路 L=L_BT+ηL_rec | mask {时间}；比例 {0.5}；η {0.1} 起步 | Δ>0 且 CI 下界>0；重建误差与下游正相关 |
| G1-D8 | D8 | 增强库替换 + L_spec | 噪声组合（先单一后组合）；SNR 宽→窄 | 干净库回落≤0.3pt 且噪声/缺失场景显著优 |
| G1-D2A | D2（ablation，挂 D3 下） | D3 + 预残差化共享源 | 共享源估计 {跨导均值, PCA 首成分} | 仅作归因，无独立晋级权 |
| G1-N1 | N1（语料轨） | 多库混合语料 + 原版 LFBT 目标 | 语料组合 {+CPSC, +CPSC+Georgia, 全混合(含 PTB/INCART)}，同 epoch 与同 step 两种预算对齐各报 | PTB-XL ≥ B0 且跨库迁移（E5-4）显著改善 |
| G1-N3 | N3 | 附加同患者正样本对（同患者+同标签过滤） | 附加对占比 {25%, 50%} | LP Δ≥1pt；患者检索 top-1 提升 |
| G1-N7 | N7（并入 D8） | LeadDropout 增强一致性（无蒸馏无教师） | k∈{1,2}；丢弃概率 {0.3, 0.5} | 干净库回落≤0.3pt 且 37 条件缺失均值显著优 |

**G-D4 硬前提细则**：D4 的每个确认配置必须同批跑「同 Mamba 主干 + 原 LFBT 目标（参数量对齐 ±5%）」对照；SD-LFBT 增益 = 相对该对照的 Δ，而非相对 VGG 基线的 Δ。

#### G2 累加消融矩阵（对齐清单 Sheet 3，编号 E008-G2-S*）

| 步骤 | 配置 | 在谁基础上 | 回答的问题 | 保留判据 |
|---|---|---|---|---|
| S0 | B0（E001 冻结） | — | 零点 | 已达成 |
| S1 | +D9 | S0 | 底座是否更稳 | G1-D9 判据 |
| S2 | +D3 | S1 | 解耦是否涨点 | G1-D3 判据 |
| S2' | D3→D1L 替换对照 | S1 | 同插槽谁强（或 S2''=D3+D1L 融合，显著优于两者才保留） | Δ 更优者入主线 |
| S3 | +D4 | S2 最优 | 时间轴是否带来节律病增益 | 分层判据 + G-D4 对照 |
| S4 | +D7 | S3 | 对比+生成是否再涨 | G1-D7 判据；不涨则移出主线、仅作附录 |
| S5 | +D8 | S4 | 鲁棒是否不伤干净库 | G1-D8 判据 |
| （S6 可选） | +D5 | S5 | 频域独立增量 | 视算力，控制算力下 Δ>0 |
| LOO | 最终模型逐模块去除 | 最终模型 | 每个模块必要性 | 去除后显著下降 |
| NEG | 随机图/打乱先验替换 D1L | S2 系 | 涨点来自正确先验而非参数 | 错误先验不涨 |

#### G3 机理与专项（对齐清单 Sheet 4，映射执行）

- **机理（E3/E4）**：E3-1 跨导相似度 vs Einthoven 理论矩阵（D3/D1L 主证据图）；E3-3 DCI 解耦质量；E4-1 病种分层增益（D4 主证据图：节律病 AF/I-AVB/LBBB/RBBB/GSVT/SB vs 形态病 MI/HYP/STTC）；E4-2 GradCAM 落区。
- **鲁棒（E5）**：E5-1 电极错位（复用 E007 T6 的 37 条件管线）；E5-3 SNR 曲线；E5-4 跨库迁移；E5-5 batch 扫描（D9 主证据图，对标论文 Table 6）；E5-6 标签效率。
- **SOTA（E6）**：同骨干同协议对比 VICReg/BYOL/DINO/TS2Vec 等主流 SSL（复用既有 run_pt 开关），外加近年 ECG-SSL。
- **效率（E7）**：参数量/FLOPs/显存/epoch 时长 vs ΔAUPRC 帕累托（D4 必答）。

### 3.4 统计与功效（先摸方差再定门槛）

- 历史 seed 方差参考：B0 FT100 std ≈ 0.2–1.2pt、FT10 std ≈ 1.3pt（E002/E007 两批）。据此：
  - **粗筛线 Δ≥1.0pt** 合理（高于两批观测噪声上界）；
  - 确认阶段以 5 seed 配对检验为主，FT10 场景若 std 仍 ~1.3pt，则该档只报趋势不单独下结论；
  - 不伤害约束 ≤0.3pt 在 PTB-XL（主库）与 CPSC 上从严执行；Georgia 若启用仅作趋势监控。
- 主指标 AUPRC（macro），AUROC 辅；LP 与 FT 分开报告，模块增益以 LP 为主口径（FT 易被下游容量掩盖）。

### 3.5 工程要求（继承 E001 工程范式）

1. 开关矩阵 + 参数账硬断言（每配置自动记录参数量/FLOPs/显存）。
2. `开关全关 == B0 逐位一致` 的单测（沿用 `test_eta_zero_is_bit_identical_to_baseline` 范式）。
3. resume 幂等 + 锁定 test 只评一次（val 选 checkpoint）。
4. 每次跑完自动汇入 RunLog（对齐清单 Sheet 6）与《创新文档》档案（E008 系列）。

### 3.6 风险与对策表

| 风险 | 概率 | 对策 |
|---|---|---|
| D3 分解塌缩（c 或 s 吞掉全部信息） | 中 | 能量比监控 + 正交约束 + 退路 D1L |
| D1L 重蹈 LGA 覆辙（LGA 拉近式显著负；E006-S1 对齐式弱正，非全族皆负） | 中 | 框架严格区分为"目标矩阵"非"拉近"；NEG 不成立立即终止该插槽 |
| D4 增益归因到 backbone 而非机制 | 中 | G-D4 参数对齐对照为硬前提，无对照的批次作废 |
| D7 与 CoRe-ECG 撞车被审稿质疑新颖性 | 高 | 定位为工程增益；写作划界：重建支路服务于导联解耦主线 |
| batch/算力不足以跑 2048 对照 | 中 | D9 的 batch 曲线按 {128, 512} 两点简化，2048 点标注为可选 |
| 三库扩展后训练时间失控 | 中 | Phase B 仅对累加矩阵 S2 起的幸存配置跑，G1 阶段留 PTB-XL |
| 连续失败后无主创新可写 | 低 | 最小可成稿组合已含 D9+D3/D1L+D4；D3/D1L 双保险在同一插槽 |
| N1 多库语料反而掉点（域差伤害） | 中 | 语料轨独立判死活：不通过则主线保持 PTB-XL 语料，N1 归档，不影响模块轨 |
| N2 合成域差引入伪特征 | 中 | 占比 10% 起步；生成时注入 HRV 与噪声变异；优先看 FT10 场景 |
| N7 被审稿混同于 E005 蒸馏 | 低 | 写作明确区分：纯增强一致性 vs 师生蒸馏；并引 E002 LeadMask 未关闭线索 |

### 3.7 里程碑与交付物

| 阶段 | 内容 | 交付物 |
|---|---|---|
| W0（并行） | 本地多库数据清洗/入库（CPSC/Georgia/PTB/INCART，零下载）；G1-N1 语料轨预训练 | N1 语料判死活；语料底座定版 |
| W1–W2 | G1-D9 粗筛+确认；batch 曲线；N4 权重 EMA 附带验证 | batch-AUPRC 双曲线图；底座定版 |
| W3–W4 | G1-D3 / G1-D1L 粗筛+确认 + NEG | 关系层定版；E3-1 热图对照 |
| W5–W6 | G1-D4（含 G-D4 对照）+ E4-1 分层 | 病种分层柱状图；时间轴定版 |
| W7 | G1-D7 冲点 | mask-性能曲线 |
| W8–W9 | G2 累加 S1→S5 + LOO | 累加瀑布图 + LOO 表 |
| W10 | G3 鲁棒/效率/SOTA + 成稿 | E5 曲线族、E6 对比表、E7 帕累托图 |

---

## 附录 A 调研文献清单

**基线**
- LFBT: Liu et al., *Information Fusion* 2025 — [Lead-fusion Barlow twins](https://www.sciencedirect.com/science/article/abs/pii/S1566253524004767)

**D9（目标函数解耦）**
- VICReg: Bardes et al., ICLR 2022（方差-协方差正则解耦，batch 鲁棒性）

**D7（掩码重建）**
- MaeFE: Zhang et al. 2023 — [Masked Autoencoders Family of ECG](https://www.semanticscholar.org/paper/fcc0a6765028b6a42dfb2fc43de927101cea532b)（100+ 引用）
- Sawano et al. 2024 — [MAE-ECG 12导联应用](https://pmc.ncbi.nlm.nih.gov/articles/PMC11324121/)
- CoRe-ECG 2026 — [对比+重建协同（引用 LFBT，需划界）](https://arxiv.org/html/2604.11359v1)

**D4（SSM/长程时序）**
- ECG-Mamba: Jiang et al. 2025 — [非标准导联心律异常检测](https://pmc.ncbi.nlm.nih.gov/articles/PMC12599890/)
- Parmonangan et al. 2025 — [自监督 Mamba ECG 与噪声鲁棒](https://pubmed.ncbi.nlm.nih.gov/41337234/)
- Hu et al. 2025 — [Mamba 长程生物信号建模](https://www.nature.com/articles/s41598-025-22684-x)

**领域天花板参照（E6 定位用）**
- Moody et al. 2025, NEJM AI — [ECG 基础模型](https://ai.nejm.org/doi/abs/10.1056/AIoa2500164)
- ECG-LFM 2026, Nature Communications — [千万级 ECG 预训练](https://www.nature.com/articles/s41467-026-72436-2)
- [Foundation Models for ECG: 混合 SSL 基准](https://arxiv.org/html/2407.07110v2)

**新轴方向（N1–N8，详见《03-数据与新轴方向调研(N1-N8).md》）**
- SimECG: [Simulator-Based Synthetic ECGs for Self-Supervised Pretraining](https://openreview.net/forum?id=gUXKz2PwQb)（N2）
- [Temporal and spatial SSL for ECG, Sci. Rep. 2025](https://www.nature.com/articles/s41598-025-90084-2)（N3，PTB-XL 21,837 条/18,885 患者结构）
- SAM 族: [SAM](https://arxiv.org/abs/2010.01412) / [F-SAM, CVPR 2024](https://cvpr.thecvf.com/virtual/2024/poster/29933) / [SSAM, JMLR 2025](https://www.jmlr.org/papers/v26/24-0065.html) / [批评, ICML 2026](https://icml.cc/virtual/2026/poster/66010)（N5）

**内部档案（证据链）**
- `LBTF/创新文档/实验档案索引.md`（E001–E005）
- `E001_lrtc_lga/E001_2026-09-08_lrtc-lga_evidence_chain.md`（E007 证据链）及 `recompute_3seed_output.txt`（三种子核验数）
- `LBTF_PhysioSpatial_Prior/00_LBTF_Baseline/ECG_SSL_LFBT-main/E006_PhysioSpatial_Baseline_Report_zh.md`（E006 完整报告：方法/结果/材料索引；runs 在 `G:\lbtf_e006_runs`）
