# SD-LFBT 实验主线设计（完整版）

> 版本：v2.4（2026-09-15：文档体系补全——新增《04》《05》引用；E006 断链已修正、历史资产已索引）
> 工作区：`E:\新实验`
> 本文件是新一轮实验的**主纲**：盘点 base 模型 → 现有方向全景 → 详细记录我们试过哪些模块 → 按这个方向还有哪些可以做 → 推进顺序与纪律。
> 模块外部文献依据见《02-模块调研与大实验设计.md》；新轴方向（N1–N8）详档见《03-数据与新轴方向调研(N1-N8).md》；工程与数据集适配见《04-工程与数据集适配设计.md》；历史实验与代码资产索引见《05-历史实验与代码资产索引.md》；历史原始档案在 `E:\LBTF创新\LBTF\创新文档\`。

---

## 一、本工作区与 Base 模型盘点

### 1.1 工作区内容

| 资产 | 状态 | 说明 |
|---|---|---|
| `ECG_SSL_LFBT-main/` | ✅ 基本完整（09-15 核验） | run_*.py、`models/`、`data_utils/` 均在，仅缺 `utils/`（354 行，自 `E:\LBTF创新\LBTF\ECG_SSL_LFBT-main\utils` 拷贝）；历史脚本（run_e003/004/005、evaluate_*、tests/）在源仓库可访问 |
| `checkpoint/ptxl_gamma08/` | ✅ 可用 | 基线预训练权重（SHA256 已登记），可直接作为下游与粗筛的对照组 |
| `data/` | ✅ PTB-XL 就绪 | 8 导联 [II,III,V1–V6]、2048 点、5 类 superclass、官方 strat_fold 划分（folds 1-8 预训练 / 9 验证 / 10 测试）；注意 `pt_pretrain/`（17,418，冻结协议用）与 `pretrain/`（21,837 全集）两目录并存，W0 审计对账 |
| `E:\数据集`（原始库） | ✅ 已清点 | PTB-XL 全集 21,837（wfdb）；Challenge2020 六库：cpsc_2018 6,877 / cpsc_2018_extra 3,453 / georgia 10,344 / ptb 516 / incart 74 / **ptb-xl 21,837（与下游同源，禁入预训练）**；MDT_DATA 判定无用，弃用 |
| `LFBT实验方向清单.xlsx` | ❌ 缺失 | 两处磁盘均未找到；W0 从本套 md 重建 |
| 02 号文档 | ✅ | 模块调研依据与大实验（E008）设计 |
| 04/05 号文档 | ✅ | 工程与数据集适配设计；历史实验与代码资产索引（E006 断链修正的源头文档） |

### 1.2 Base 模型（LFBT）构成

LFBT = Liu et al., *Information Fusion* 2025（官方仓库 Aiwiscal/ECG_SSL_LFBT）。代码级的模型组成：

| 组件 | 具体形态（本工程配置） |
|---|---|
| **Encoder** | 8 个**互相独立**的 VGG16 风格 1D-CNN（`VGG16(ch_in=1, alpha=0.125)`），每个导联一个，输出 64 维 |
| **Projector** | 每导联独立 3 层 MLP（2048-2048-2048） |
| **融合方式** | MBC：各导联表征 concat（论文消融优于单 CNN） |
| **损失** | `L = γ·L_r + (1−γ)·L_t`，γ=0.8；intra-lead（各导联自视图冗余削减）+ inter-lead（跨导联相关矩阵压向 0，非对角权重 λ=0.0051）双 Barlow Twins |
| **增强** | RRC-TO（随机尺寸裁剪 + 时间遮挡）构造正样本对 |
| **优化** | Adam lr=1e-3，batch=128，200 epoch（注：论文最优 batch=2048，专用服务器按 RTX 4090 24G 协议执行用 128；本地笔记本仅开发/单测，不跑训练） |
| **下游** | LP（冻结+线性探测，lr 1e-3）/ FT（全量微调，lr 1e-4），100 ep，val 选点、test 只评一次 |

### 1.3 基线复现锚点（E001，2026-08-07，seed 0）

| 任务 | AUROC | AUPRC | 论文参考 AUPRC |
|---|---:|---:|---:|
| LP 100% | 0.9126 | **0.7177** | 0.6923 |
| FT 100% | 0.9197 | **0.7173** | 0.7257 |
| FT 10% | 0.8756 | 0.6431 | — |
| LP（随机初始化对照） | 0.4700 | 0.1930 | — |

复现判定：偏差 < 1pt，✅ 成立。多 seed 口径的历史 B0（E002 批，3 seed）：LP 0.7137±0.0036 / FT100 0.7200 / FT10 0.6487。

### 1.4 数据与算力约束（决定哪些方向"能做"）

1. **预训练语料受限**：冻结协议用 PTB-XL folds 1-8（17,418 条）替代论文的 NFH；**NFH/Chapman 判定为私有数据集，弃用（2026-09-15 定）**，语料扩展改走本地多库路线（N1：+CPSC 10,330 +Georgia 10,344 +PTB 516 +INCART 74 ≈ 38,700 条，零下载）；**SNPH 保密不可得** → 原清单里依赖 SNPH 的"真实无预处理鲁棒"故事（D8/E5-2）需改道：加性噪声 SNR 曲线 + 电极错位（37 条件管线已有）+ 跨库迁移（PTB-XL ↔ CPSC）。
2. **batch 上限 128**（论文最优 2048）：BT 目标处于最弱区间 → 这正是 D9 的最大机会（见 4.1）。
3. 硬件：正式训练在专用服务器（协议按单卡 RTX 4090 25.8GB 设定；本地 3050 Ti 4GB 仅作开发/单测）→ 换主干（D4）、双分支（D5）必须过显存与效率账（E7）。

---

## 二、现有方向全景（三轴 × D1–D10）

对 LFBT 的升级按"改什么"分为四个插槽、三条正交轴：

```text
导联轴（关系目标层①）    ：D1 生理拓扑目标  D2 偏相关  D3 共源-特异解耦
时间轴（编码器层②）      ：D4 Mamba 全局主干  D5 时-频双分支  D6 多尺度 multi-crop
目标轴（视图层③）        ：D7 掩码重建  D8 物理安全增强
目标轴（损失/优化层④）   ：D9 VICReg 化  D10 EMA 自蒸馏+原型
```

各插槽可跨插槽叠加，同插槽内是替代/递进关系（D1/D2/D3 竞争上岗，D4/D5/D6 同理）。

**模块轴之外的四条新轴（v2.1 新增，详档见 03 号文档）**：数据轴（N1 多库语料、N2 合成数据）、采样轴（N3 患者级正对）、优化轴（N4 权重 EMA、N5 SAM）、鲁棒/元数据轴（N7 导联丢弃、N8 元数据头）。新轴与模块轴正交——历史七轮负结果全部发生在模块轴上，不覆盖新轴。

---

## 三、我们试过哪些模块（E001–E007 详细记录）

> 结论先行：**七个实验轮次里，所有"加辅助项/轻适配器/注意力重加权"类尝试全部未过噪声线，无一存活进主线。** 证据链存于 `E:\LBTF创新`。

### E001（08-07~08）｜LFBT 基线复现 —— ✅ 成立
- 做了什么：按论文协议复现 LFBT（见 1.2/1.3），冻结协议、登记 checkpoint。
- 结论：基线零点确立，所有后续 Δ 都以它为分母。

### E002（08-09~11）｜AR-LFBT 候选模块筛选（B1–B4 变体）—— ❌ 无一稳定提升
- 试了什么模块：以**导联注意力/通道加权/融合改造**为主的四个变体（B1 Attention 类、B2 ECA 类、B3 Mean 融合、B4 Fusion-BT 类），3 seed 对比 B0。
- 结果：全部未稳定超过 B0；B3（Mean 融合）基本持平，留作 AR-B3 对照基线；Lead Mask 一项待独立复验后搁置。
- **判定：关系层走"注意力/重加权"这条路关闭。**

### E003（08-11~13）｜六类跨领域模块单消融 + 自动复合 —— ❌ 失败
- 试了什么模块：GRN（轻量时域归一化适配器）、Mixer（导联混合）、JEPA（潜空间预测）等六类跨领域模块，先单插消融再自动复合。
- 结果：GRN 仅"低成本趋势"信号；**Mixer 明显退化**；JEPA 部分缓解但未回到 B0；自动复合全部失败。
- **判定：①往 LFBT 上堆跨领域小模块不成；②"导联混合"类（功能上类似重 GAT 的消息传递）直接退化，进一步否定 D1 重版。**

### E004（08-13~17）｜GRN-LFBT 专项验证 —— ❌ 方法学路线终止
- 试了什么模块：GRN 时序适配器专项、5 seed、参数匹配与外部验证。
- 结果：FT-10 仅小幅趋势，机制归因不足 → 终止论文路线（后改写为 LRTC-Net 应用论文单独投稿，不占主线）。
- **判定：时间轴"小修小补"不成，要做必须换真主干（→ D4 Mamba）。**

### E005（08-17~19）｜可靠性感知跨导联掩码蒸馏（RCLMD）—— ❌ 证伪
- 试了什么模块：完整导联教师 → 受损学生的跨导联掩码**蒸馏**框架，含可靠性/拓扑加权（R01→R06 六组配置）。
- 结果：R06 vs R01：clean **−0.023**、双导联缺失 **−0.009**，负增益。
- **判定：师生蒸馏族在本地协议下证伪 → D10（EMA 教师+原型）放弃的直接依据。**

### E006（08-24 设计 → 09-08 formal 完成归档）｜结构化生理-空间先验（P1/S1）—— ⚠️ 已跑完：弱正未过粗筛线，方向未证伪
- 做了什么：P1 波形-log-STFT 谱对齐 + S1 解剖分组（下壁(II,III)/前壁(V1–V4)/侧壁(V5,V6)）跨视图 BT 对齐，预注册 2×4 消融（基线 B0/AR-B3 × baseline/physiology/spatial/full）× 3 seed × {200ep 预训练, LP, FT100, FT10}，**24/24 全流程完成**（报告：`E:\LBTF创新\LBTF_PhysioSpatial_Prior\00_LBTF_Baseline\ECG_SSL_LFBT-main\E006_PhysioSpatial_Baseline_Report_zh.md`；runs 在 `G:\lbtf_e006_runs`。设计中的 S2 自适应关系矩阵、S3 关系感知 token 未跑）。
- 关键结果（3-seed test）：**增益基线依赖**——AR-B3（mean 融合+LeadMask 鲁棒基座）上 full LP **+0.0040±0.0019（3/3 seed 为正）**、AUPRC +0.0104（0.7146 vs 0.7042，全场最高），P1/S1 单开均值亦全正（符号一致 2–3/3）；B0 上 full LP **−0.0016（0/3）**，无增益。
- **判定：①量级 0.2–0.4pt < 1pt 粗筛线，B0 上不进主线；②方向未被证伪**——在鲁棒融合基座上全协议一致为正，机制解释为"先验补偿鲁棒训练丢失的谱形态/解剖结构"；③局限：n=3、无正式显著性检验。
- 对主线的三个联动：a) **D5 前哨已发射**——P1 弱正（AR-B3 FT100 3/3 +0.0027），D5 维持第二梯队但信号未死；b) **D1L 的"S1 为负则插槽判给 D3"触发器未触发**（S1 弱正非负），D1L 保留竞争资格；c) E006-S1（BT **对齐**式分组）弱正 vs E007-LGA（**拉近**式分组）显著负——反差支持"结构化对齐/去相关目标"而非"拉近"框架。
- 附注（协议可比性）：E006 formal 在另一台机器跑（RTX 3080 10G、torch 2.5.1+cu121、batch=96/workers=0，screen 后确认无损），下游用 `data/ptbxl` 患者隔离划分（17,092）——与 E001 线的 `data/downstream`（22,299）是**两套划分体系**，W0 审计必须对账定版（见《04》W0-3⑤）。

### E007（09-08）｜LRTC-Net(LGA) 证据链（M0–M3，2×2 正交开关）—— ❌ LGA 无增益
- 试了什么模块：**LGA = 导联解剖分组对齐辅助损失**（零参数，分组 (II,III)/(V1–V4)/(V5,V6)，组内拉近）× TRC(=GRN) 时序适配器，M0 基线 / M1 仅LGA / M2 仅TRC / M3 双开，3 seed（0,2,4）配对核验。
- 关键结果：
  - M1−M0（仅 LGA）ft_100 **−0.0089**，95%CI [−0.0162, −0.0020] —— **显著为负**；
  - M3−M2（TRC+LGA）ft_100 +0.0025 不显著；ft_10 **−0.0048** 显著为负；
  - T6 导联缺失 37 条件下 M3 仅 +0.003 边际；T5 η 扫描无稳定档位。
- **判定：①"把同组导联拉近"（invariance 方向）方向错误；②再次确认轻量辅助项淹没在 seed 噪声（±0.5~1pt）之下。**

### 试错总账（按模块族归并）

| 模块族 | 对应清单编号 | 实验出处 | 结果 |
|---|---|---|---|
| 导联注意力/通道加权/融合 | （E002 自提，否决 D1 重版） | E002 | ❌ 无稳定提升 |
| 导联混合 Mixer / token 交互 | 否决 D1-GAT | E003 | ❌ 明显退化 |
| 轻量时序适配器 GRN/TRC | D4 的轻量替身 | E003/E004/E007 | ❌ 趋势不足，终止 |
| 师生掩码蒸馏 | D10 同族 | E005 | ❌ 证伪（负增益） |
| 导联分组对齐 LGA | D1 的"拉近"变体 | E007 | ❌ 显著为负 |
| 波形-STFT 一致 + 分组一致 | D5/D1 前哨 | E006 | ⚠️ 已跑：AR-B3 上弱正（full LP +0.0040，3/3）、B0 无增益，未过 1pt 粗筛线；方向未证伪 |
| **从未试过** | **D9 / D3 / D4(真主干) / D7(重建式) / D8** | — | **← 剩余全部机会在这里** |

---

## 四、按这个方向还有哪些可以做

> 筛选逻辑：①外部文献证据充分；②未被 E002–E007 任何负结果覆盖；③直击 1.4 的本地约束。满足全部三条的只有五个，按优先级排列。

### 4.1 D9：VICReg 式方差-协方差解耦 + 白化（第一优先，底座）🟢

- **做什么**：把 BT 目标拆成 invariance + variance hinge（防塌）+ covariance 去相关三项，BN → LN/白化。
- **为什么能做**：a) 本地 batch=128 正处于论文 Table 6 曲线最弱区间，D9 是唯一正面解决该约束的方向；b) VICReg 的 batch 鲁棒性有充分外部证据；c) 与历史任何负结果不同族（改的是目标函数结构，不是加辅助项）。
- **胜条件**：batch 128 下不劣于原 BT 且 batch 曲线更平——即使下游 Δ≈0，"小 batch 解放"本身可写。
- **成本**：S-M，改动集中在损失与归一化，不动数据管线。

### 4.2 D3：共源-特异解耦（方法学主创新）🟢

- **做什么**：z^i = [c ; s^i] 分头，共性 c 跨导联一致（承接 intra 约束），特异 {s^i} 去相关（承接 inter 约束），c⊥s 正交防串味。
- **为什么能做**：a) E007 证明"硬压 0/硬拉近"两个极端都错，D3 恰是中间态（保留共性、只去特异冗余），与 LGA 的失败互补而非重叠；b) 多视图 common/specific 分解外部方法族成熟，塌缩有标准监控；c) ECG 导联级尚无同类工作，新颖性空位。
- **配套机制证据**：c 在导联置换下的稳定性（E3-3）直接支撑电极错位鲁棒（E5-1 复用 E007 的 37 条件管线）。
- **退路**：若塌缩不可控 → 退 D1L。

### 4.3 D1L：结构化目标矩阵（轻量，与 D3 同插槽竞争）🟡

- **做什么**：inter-loss 的目标矩阵从全 0 改为生理拓扑设定（II-III 按 Einthoven 夹角、相邻胸导非零目标），**只改目标值，不加模块**。
- **为什么还能做（LGA 失败后）**：LGA 是"组内拉近"（invariance），D1L 是"结构化去相关目标"（目标相关值），机制相反；E002/E003 否决的是"加注意力/混合模块"，D1L 零新参数不受覆盖。
- **必要自证**：NEG 负对照（随机图/打乱先验）不涨才有效。E006-S1 已实跑为弱正（非负），"为负判 D3"的触发器未触发，D1L 保留竞争资格；且 S1（对齐式）弱正 vs LGA（拉近式）显著负的反差，支持本方向坚持"结构化对齐目标"框架。

### 4.4 D4：Mamba 全局时序主干（有条件，带硬前提 G-D4）🟡

- **做什么**：CNN 保形态 + Mamba 建长程节律，L_temp 约束相邻时段/beat 全局表征一致。
- **为什么能做**：E004 已证明轻量适配器不成——时间轴要么换真主干要么放弃；ECG-Mamba 类外部证据充分（线性成本适配 4090）。
- **硬前提 G-D4**：必须同批跑"同 Mamba 主干 + 原 LFBT 目标、参数量对齐 ±5%"对照，把 backbone 增益与机制增益分开，否则整条轴结论无效。
- **预期落点**：节律病类（AF/传导阻滞/GSVT）Δ 显著大于形态病类（E4-1 分层是主证据图）。

### 4.5 D7：掩码重建 MIM（冲点，仅重建式）🟢

- **做什么**：对比支路外并行时间掩码重建支路，L = L_BT + η·L_rec，mask 0.5、η 0.1 起步。
- **为什么能做**：E005 证伪的是**蒸馏式**掩码（师生框架），重建式（自监督 MAE 族）机制不同不受覆盖；MaeFE/MAE-ECG 外部证据强。
- **定位**：工程冲点而非主创新（CoRe-ECG 2026 已做 12 导联对比+重建，写作需划界）。

### 4.6 D8：物理安全增强 + 频谱一致（防御）🟢

- **做什么**：RRC-TO 之上加物理噪声库（基线漂移/工频/肌电/幅度缩放/时间扭曲）+ 0.5–40Hz 带内频谱一致损失。
- **本地改道**：SNPH 不可得 → 鲁棒评估改用加性噪声 SNR 曲线 + 37 条件电极错位 + 跨库迁移（PTB-XL ↔ CPSC）。
- **定位**：防御/落地叙事，判据宽松（干净库回落 ≤0.3pt + 场景显著优）。
- **联动（E006 新证据）**：P1/S1 先验在鲁棒基座（AR-B3=mean+LeadMask）上呈正交互作用（full LP +0.0040，3/3）→ D8/N7 确认阶段可选附检"+P1/S1"附录级配置，不占主线。

### 4.7 第二梯队与弃用清单

| 模块 | 决策 | 依据 |
|---|---|---|
| D5 时-频双分支 | ⏸ 第二梯队 | S3 后视算力；E006-P1 前哨已跑：AR-B3 上弱正（FT100 +0.0027，3/3）、B0 无增益——信号未死但弱，维持第二梯队 |
| D2 偏相关 | ⬇ 降级为 D3 的 ablation | batch 128 下协方差求逆噪声大 |
| D6 多尺度 multi-crop | ❌ 弃 | 短窗正样本语义不一致（结构性缺陷）+ E003 同族不乐观 |
| D10 EMA 蒸馏+原型 | ❌ 弃 | E005 证伪同族 + 调参成本最高 |
| D1 重 GAT 版 | ❌ 弃 | E002/E003 双重否决（注意力族 + 导联混合族） |
| E002–E005 旧方向 | ❌ 不再投入 | 见第三部分 |

### 4.8 新轴方向（模块轴之外的八条路，详档见 03 号文档）

| 方向 | 轴 | 一句话 | 决策 |
|---|---|---|---|
| N1 多库联合预训练 | 数据 | 本地多库混合语料（PTB-XL+CPSC+Georgia+PTB+INCART ≈3.87 万条，零下载） | ✅ 底座级，语料轨并行 |
| N2 合成数据预训练 | 数据 | 参数可控的仿真 ECG 混入预训练（SimECG 证据） | ✅ 冲点/机理故事 |
| N3 患者级正样本对 | 采样 | 同患者不同记录互为附加正对 | ✅ 低成本增广 |
| N4 权重 EMA/soup | 优化 | 末段权重平均，不改损失不加分支 | ✅ 免费附件，默认开 |
| N7 导联丢弃一致性 | 鲁棒 | 纯增强式导联置零（E002 LeadMask 线索未关闭） | ✅ 并入 D8 |
| N5 SAM/F-SAM | 优化 | 锐度感知优化器（训练×2，需防虚报） | ⚠️ 第三梯队 |
| N6 多层辅助 BT 损失 | 表征深度 | projector 中间层加辅助损失 | ⚠️ D9 延伸项 |
| N8 元数据辅助头 | 元数据 | age/sex 回归辅助（牺牲纯 SSL 定位） | ⚠️ 附录/防御 |

明确不开：ECG-文本跨模态（无文本+算力）、backbone scaling study（天花板已封）、聚类/师生蒸馏族（E005 证伪）。

---

## 五、主线推进顺序（六步 + 语料轨，编号 E008 起）

```text
S0   E0 基线零点（✅ 已完成：checkpoint + 冻结协议）
 ↓   【开工前置：补齐 models/ data_utils/ utils/ 三个目录】
S0.5 【并行·语料轨】N1 多库数据入库 + 多库预训练验证（PTB-XL≥B0 且跨库改善 → G2 起切换语料）
S1   +D9   底座：目标函数三项化 + 白化（N4 权重 EMA 全程默认附带）
S2   +D3   关系层主创新（D1L 同插槽对照 + NEG 负对照）
S3   +D4   时间轴真主干（带 G-D4 参数对齐对照）
S4   +D7   掩码重建冲点（N2 合成数据同期粗筛，配机理归因图）
S5   +D8   物理增强防御（并入 N7 导联丢弃，构成"噪声+缺失+错位"三线鲁棒故事）
 ↓
成稿：累加瀑布 + LOO 留一 + NEG + 分层/鲁棒/效率/SOTA（N3 患者级正对在 D9 底座上择机插入）
```

- **最小可成稿组合**：E0 + D9 + (D3 或 D1L) + D4；语料轨通过则关键配置在 N1 语料上复跑一遍。
- 每步先粗筛（1–2 seed，PTB-XL LP，Δ≥1.0pt 过线）再确认（**3 seed**；5 seed 只对最终旗舰配置后补——2026-09-15 定版，纯计算 ~81h/全主线；跨库评估在 PTB-XL + CPSC 上）。
- 详细实验矩阵、统计功效、里程碑排期见《02-模块调研与大实验设计.md》第三部分（E008 立项）；新轴接入细则与正交性核对见《03》第四节。

## 六、全程纪律（红线）

1. 唯一变量：只改目标模块，其余与 E0 逐位对齐（开关全关必须与 B0 逐位一致，沿用单测硬验证）。
2. 两段式筛选，粗筛不过直接归档终止，照记 RunLog，不选择性报告。
3. 统计口径：5 seed 均值±std + 配对检验 + bootstrap 95%CI；有效 = 均值为正且 CI 下界 > 0。
4. 不伤害约束：新库涨点时其余库回落 ≤0.3pt。
5. 归因自证：先验/结构类模块必须带负对照；换主干必须带参数对齐对照。
6. 档案续接：新实验从 E008 编号，归档回《创新文档》体系；本表与 xlsx 台账同步更新。
