# 每小时巡检报告(主机A)— 2026-09-18 16:00

## ★ 重点判读:proj4096 超重足迹压住 A-P2 主力(非故障,需主会话知情)

- `proj_dim4096` 实测显存足迹 **~14.7G**(预估 ≤9G 偏乐观),单进程即把利用率打到 **98%**;
- 后果一:`h2_blur3` s0/s2 已候闸 **70 分钟**(空闲 7.3G < 闸门 7.8G),且该状态预计持续到 proj4096 跑完;
- 后果二:proj4096 实测 **159s/ep**(4096³ 头极重),epoch 28/200,**PT ETA ~23:00**,判定行 ~23:40;
- **巡检判读:无需干预**——SM 已 98% 饱和,强行塞入 h2(降闸门至 6500)不会增加总算力吞吐,只会摊薄 proj4096 速度并把显存余量压到 ~0.6G(风险大于收益)。代价仅是**读数顺序**:h2 seed0 首个读数从 ~19:00 推迟到 ~23:00 后,收官估算整体顺延 3–5h 至 **09-19 傍晚–夜间**;
- 若主会话认为 h2 读数优先级必须提前,唯一手段是中途放弃 proj4096(已烧 30 epoch,不建议),维持现状为优。

## 一、GPU 与车道

- 16,815/24,564 MiB(68%),利用率 98%;3 runner 存活(CIM 核验);
- run_pt ×1 = proj_dim4096(pid 44196);laneF/G 空转候闸(持有 h2 s0/s2 认领);claims 3 个均对应在跑/候闸任务;
- 零 OOM/Traceback;watchdog_relay 保持废弃。

## 二、在跑任务与 ETA

| 任务 | 进度 | 速度 | ETA |
|---|---|---|---|
| `proj_dim4096` | epoch 28/200 | 159s/ep | PT ~23:00;判定 ~23:40 |
| `h2_blur3` s0/s2 | 候闸(等 proj4096 释放) | — | 入场 ~23:00;**s0 判定 ~09-19 凌晨 2–3 点** |
| 后续 | h2 s4 → t3 ×3 → h1 ×3+2NEG → 队尾 5 项 | — | 收官顺延至 09-19 傍晚–夜间 |

## 三、事故与处置

无事故;无自愈动作(候闸车道均为活进程,不属于重启范围)。

## 四、自上小时新完成

无(csv 维持 20 数据行;d7_rec01 ❌ −0.0113、proj_dim1024 ❌ −0.0085 均已在上轮记录)。

## 五、告警与待办

1. 【主会话知情项】上表"重点判读"——h2 读数顺延 ~4h、收官顺延 3–5h,维持现状建议;
2. 【git】15:05 网络断(本条随本轮巡检重试,结果见巡检结论);
3. 挂起不变:merge 远端 main / laneC-G 并发对齐 / 巡检去重闸。

---

## ⚠️ 16:05-16:10 更正与处置(第二巡检实例,CPU 采样抓出并行实例误判)

- **proj_dim4096 实为挂死**:CPU 时间冻结(2523s→2523s/8s)、日志停在 15:40 epoch28 且无错误输出——98% GPU 利用率是僵尸 CUDA 上下文空转假象,上节"重负载压闸"判读不成立;
- **处置**:16:05 击杀僵尸 pid 44196 → 释放 14.7GB → 候闸 70 分钟的 h2_blur3 seed0/seed2 于 16:03 前后入场;16:03 laneC(旧内存队列,不含 A-P2 条目)趁乱抢入 speed_perturb 致三探针并存(23.9GB/195MB 危险满载)→ 16:08 让位击杀 speed_perturb(仅 2 分钟龄,claim/半成品已清,laneC 稍后自行重试);
- **当前**:h2_blur3 s0 + s2 双探针运行(17.7/24.5,余 6.4GB)✓;proj_dim4096 待重跑(约 14.7GB 足迹,建议独占窗口跑);
- **判活规则再强化**:GPU util 高不等于进程活,**CPU 采样是唯一可靠判据**(本轮即为例证)。
