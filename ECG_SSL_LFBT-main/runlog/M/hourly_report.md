# 每小时巡检报告(主机A)— 2026-09-18 15:00

## ★ 本小时新判定(2 行,矩阵增至 20 数据行)

- **`d7_rec01` ❌ 14:28**:AUPRC 0.7064,Δ −0.0113(d7-weight 0.1)。D7/MAE 重建支线两剂量全负(0.03: −0.0019;0.1: −0.0113)→ **该轴收官关闭**;
- **`proj_dim1024` ❌ 14:50**:AUPRC 0.7092,Δ −0.0085(投影头加宽至 1024³ 有害)。投影头宽度轴剩 proj_dim4096 在跑,若同样为负则整轴关闭。

两 PT 均干净跑完 epoch 200 + LP,零 Traceback;车道自动落行无误。

## 一、GPU 与车道

- 显存 16,808/24,564 MiB(68%),利用率 95%;proj_dim4096 正在爬坡,空闲 ~7.7G;
- 3 runner 存活;run_pt ×1 = proj_dim4096(pid 44196,~14:26 过闸启动,laneC 阻塞于子任务属正常);
- **A-P2 主力开抢**:laneG 认领 `h2_blur3_seed0`、laneF 认领 `h2_blur3_seed2`,均候 7800MiB 闸门(当前 2322–7369),proj4096 足迹稳定后预计双双入场 → 三车道满载。

## 二、在跑任务与 ETA

| 任务 | 进度 | ETA |
|---|---|---|
| `proj_dim4096`(laneC,~14:26 起) | 早期 epoch,4096³ 头偏重(~60–90s/ep 估) | PT ~18:00–19:30,判定行随后 |
| `h2_blur3` s0(laneG)/ s2(laneF) | 已认领候闸 | 入场 ~15:10–15:30;**s0 判定 ~18:30–19:30** |
| 后续 | h2 s4 → t3_pow3 ×3 → h1 ×3+2NEG → 队尾 5 项 | 见收官估算 |

## 三、事故与处置

无事故、无自愈动作;OOM/Traceback = 0;watchdog_relay 保持废弃。

## 四、告警与待办(不变项从简)

1. 【观察项】proj_dim4096 的 4096³ 投影头显存足迹未实测,若 >9G 可能压缩 h2 并发为 2 道——非故障,巡检持续观察;
2. 挂起:① merge 远端 main;② laneC/laneG 并发上限对齐;③ 巡检去重闸(工具暂缺);
3. 收官估算(主会话口径):队列清空约 09-19 中午–傍晚;若队尾单 seed 探针转正各 +2 确认轮顺延。
