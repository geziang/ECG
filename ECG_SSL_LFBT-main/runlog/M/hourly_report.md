# 定时巡检报告(主机A)— 2026-09-21 19:45(事故处置轮)

## 一、GPU

21,069 / 24,564 MiB(86%),利用率 95% —— 确认矩阵三预训练恢复满载。

## 二、事故与处置(19:25 巡检发现 → 主会话手术)

- **现象**:三实例 16:51 启动后 2.5h 零 epoch 落盘、目录全空;py-spy 定位卡在 run_pt.py:723 的 torch.save(train_state.pth)——GPU 张量直接序列化在三进程并发下病态变慢(基准复现:CPU 240MB 7.2s vs GPU 240MB 29.3s,三实例互踩后 2.5h 未完成一次保存);
- **根因**:我为断点续训新增的每 epoch 状态保存,快照未先搬 CPU——我的功能引入的事故,与主机B无关;
- **处置**:击杀三实例(2.5h 损失,无有效进度可保)→ 修 run_pt(快照+优化器态整体 .cpu() 后序列化 + 降频至每 5 epoch + 末 epoch)→ 冒烟(3ep+resume)→ 19:32 重启三实例 → 19:45 验证三实例全部正常落盘(ep4/ep4/ep9),GPU 95%。

## 三、在跑任务

| 实例 | 进度(19:45) | 粗估 ETA |
|---|---|---|
| c2 s0(→s2→s4) | ep4/100 | 三seed ~次日凌晨 |
| c1 s0(→s2→s4) | ep4/100 | 同上 |
| b0 s2(→s4) | ep9/200 | 同上 |

下游(CPSC LP→FT10→PTB LP→Chapman)由驱动器自动接续。

## 四、新完成

无新判定;matrix 42 行不变。远端 3 个 docs 提交(状态总表+日程)已只读审阅:我们领先日程约 2 天,方向一致;待空闲窗口 merge。

## 五、动作

巡检发现异常→主会话手术(杀/修/重启,详见二);本报告随修复提交。

## 六、告警与待办

1. 【观察】下次巡检核对三实例 epoch 增速正常(参考 b0 ~5ep/7min);
2. 【待办】远端 docs 合并留空闲窗口;下游读数明晨出。
