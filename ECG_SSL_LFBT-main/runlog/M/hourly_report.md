# 定时巡检报告(主机A)— 2026-09-21 07:48

## 一、GPU

738 / 24,564 MiB(3%),利用率 0% —— 缺导扫描已完成后 GPU 空闲(mimic 未回归;无在跑训练)。

## 二、进程与车道

- 无 run_pt/run_lp/runner 进存活;lane{F,G2,C} 不存在属正常(队列空);缺导扫描 python 已干净退出;
- 扫描进程零残留,无僵尸。

## 三、缺导扫描:✅ 完成

- **SWEEP_DONE,missing_lead_curves.csv 75 行(74 条件 × 全)全部落袋**,早于 ETA(07:48 前已完成);
- 末两行示例:c1 miss57 0.6558 / miss67 0.6481;基线自洽(b0 full 0.7173 ≈ 锚点 0.7177)。

## 四、新完成(自上时点 03:00)

期间大事记(主会话执行,均有 git 提交):A线粗筛三探针全灭关线(a1 −7.06/a2 −9.11/a3 −1.83pt);主机B W1 四提交合并(940bced,test_w1 14/14);C3 外部库就绪+跨库表落账(c3_evals.csv);run_lp 加 --zero-leads;**缺导 74 条件扫描完成**。matrix csv 42 数据行无新增(缺导读数在独立 csv,不入筛选矩阵)。

## 五、本小时采取的动作

纯巡检,无自愈写操作(无需重启任何进程)。

## 六、告警与待办

1. 【待办·主会话】GPU 空闲:下一棒=T0 val 纪律代码(CPU)、run_pt 断点续训改进(CPU)、缺导曲线分析报告(CPU)、C2(C1+TRC)调研;B 线为主机B任务;
2. 【观察】mimic(他人)仍未回归;
3. 【git】巡检提交后例行推送(main 或 hostA-sync 兜底)。
