# 每小时巡检报告(主机A)— 2026-09-18 19:00(含 18:00 双实例迟到版合并)

## ★ 本小时新判定:A-P2 首个读数落地

- **`h2_blur3` seed0 ❌ 18:59**:AUPRC 0.7085,Δ −0.0092(抗混叠 BlurPool 首种子为负;预注册 3 seeds,seed4 不受影响照常排队);
- `h2_blur3` s2:PT 200ep 已完(18:59),LP 进行中,**判定 ~19:25**;
- 矩阵增至 21 数据行。

## ★ 资源事件:三任务挤入,GPU 贴线 + OOM 窗口预警

- 18:55–18:59 连锁换班:asym_view(laneC)入场 → h2_s0 判定落行 → h2_s2 转 LP → **laneG 于 18:59 接走 proj_dim4096 重试**(空闲 12939MiB 过闸);
- GPU **23,426/24,564 MiB(95.4%)**,距告警线 23500 仅 74MiB;构成=proj4096 爬坡(峰值 ~14.7G)+ asym_view(~6.7G)+ h2_s2 LP(~2.5G)+ 桌面;
- **OOM 风险窗口 ~19:10–19:45**:若 proj4096 爬坡快于 LP 退出可能瞬时破 24.5G;若发生,受害方按机制重排重试(非灾难)。LP 退出后稳态 ≈23.5G 勉强可容,**第三车道将被饿到 ~22:15(asym 出槽)**——A-P2 接力(h2_s4/t3/h1)夜间降速,收官估算维持 09-19 午后–傍晚;
- 巡检不干预(不杀活任务);20:00 巡检重点复查 proj4096 是否二次阵亡。

## 一、GPU 与车道

- 23,426/24,564 MiB(95.4%),利用率 96%;3 runner 存活;run_pt ×2(asym_view/proj_dim4096)+ run_lp ×1(h2_s2);
- laneF 阻塞于 h2_s2 LP(正常);claims 对应在跑任务;零 Traceback;watchdog_relay 保持废弃。

## 二、在跑任务与 ETA

| 任务 | 进度 | ETA |
|---|---|---|
| `h2_blur3` s2 LP | PT 已完 | **判定 ~19:25** |
| `asym_view` s0(laneC) | 刚入场(~56–60s/ep 估) | PT ~22:15;判定 ~22:40 |
| `proj_dim4096` 重试(laneG) | 刚入场(~159s/ep) | PT ~03:50(**存活观察中**) |
| h2_s4 / t3 / h1 系 | 排队候显存 | ~22:15 起接力 |

## 三、事故与处置

无新增人工处置;speed_perturb 秒败未复现(尚未再轮到)。

## 四、告警与待办

1. 【20:00 复查】proj4096 是否在 OOM 窗口二次阵亡(第 2 次失败还剩 1 次重试;再败建议主会话以"4096³ 不稳定"关轴);
2. 【主会话】h2 s0 ❌ 后,若 s2/s4 亦负 → h2 线 3-seed 关闭(自动判,无需人工);
3. 【git】18:05 断网未推(413924c 在本地),本轮重试结果见巡检结论;
4. 挂起不变:merge 远端 main / 巡检去重闸。
