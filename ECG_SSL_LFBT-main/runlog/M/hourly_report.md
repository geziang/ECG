# 定时巡检报告(主机A)— 2026-09-21 21:55

## 一、GPU

21,072 / 24,564 MiB(86%),利用率 92% —— 三预训练满载,零异常。

## 二、确认矩阵进度(19:32 重启后 2.4h)

| 实例 | 进度 | 备注 |
|---|---|---|
| c2 | **s0 已完成(100ep)** → s2 进行中 | 状态保存修复后全速 |
| c1 | **s0 已完成(100ep)** → s2 进行中 | 同上 |
| b0 | s2 至 ep169/200 | ~10min 内完成转 s4 |

修复后实测吞吐: NFH 100ep ≈ 2.3h/seed(三并发), B0 200ep ≈ 2.5h —— 比事故前预估快近一倍。粗估: 预训练全线完成 ~02:00, 下游(CPSC LP→FT10→PTB LP→Chapman)自动接续, 明晨出三种子配对表。

## 三、新完成

无新判定行(matrix 42 行不变);c2/c1 seed0 的 checkpoint 已落 checkpoint/confirm/(encoder_group.pth+config.json 全元数据)。

## 四、动作

纯巡检,无写操作(三实例健康)。

## 五、告警与待办

1. 【观察】零 FAIL 零停滞;远端 3 个 docs 提交待空闲窗口合并(纯 .md 无风险);
2. 【预期】明晨巡检应见 confirm_results.csv 开始累积(预估 20 行: 2ckpt×3seed×cpsc+ft10+ptb+chapman)。
