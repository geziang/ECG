# 每小时巡检报告(主机A)— 2026-09-18 23:05(22:00 提示词三投递,执行一次)

## ★ 本小时新判定:h2_blur3 三种子收官,门不一致关线

| seed | AUPRC | Δ | 判定 |
|---|---|---|---|
| 0 | 0.7085 | −0.92pt | ❌ |
| 2 | 0.7217 | +0.40pt | ✅ |
| 4 | 0.7149 | −0.28pt | ❌ |

- **终判:2 负 1 正,3-seed 符号一致门不通过 → h2(抗混叠 BlurPool)线关线**(与 20:00 决策级更正预期一致;种子散布 1.32pt ≫ 效应量,+0.40 判为噪声);
- 矩阵现 25 行(24 数据行);A-P2 主攻希望转移至 t3_pow3(见下,当前受阻)与 h1_white8。

## ★★ 重大异常:t3_pow3 的 LP 阶段代码级崩溃(需主会话,有 2.7h+ 白烧风险)

- **现象**:t3_pow3_seed0 PT 正常跑完(checkpoint 21:46 落盘),LP 于 21:48 崩:`RuntimeError: Expected target size [128, 5], got [128]`,栈=`run_lp.py:187 classify → 模型 forward → CrossEntropyLoss`;LP 日志仅 5KB 无 AUPRC;
- **根因(定位)**:`models/vgg_1d.py:77/81` 的 `pool_power`(Q=3 幂均值池化)输出形状带多余单例维([B,1,5] 级),PT 阶段对比学习不涉 CE 故无恙,LP 的分类 CE 即崩 → **确定性 bug,重跑必复现**;
- **自动重试的代价**:laneG2 已把 s0 重排队首(21:48,"第 1 次重试插队首"),正等显存——**一旦槽位释放(~01:45)将重烧 2.7h 全量 PT,然后在 LP 同一处再崩**;
- **同样受影响**:t3_pow3_seed2 正在跑 PT(pid 55440,ep 27@22:44),其 LP 阶段(~04:30)预计同样崩溃 → 再触发重排再白烧;t3_s4 在队列中;h1_white8 系是否同病未验证(待观察);
- **建议(主会话决策,巡检无权改码/杀任务)**:
  1. **尽快**(最好 ≤01:40,赶在 s0 重跑开跑前、s2 LP 前)修 `models/vgg_1d.py` 幂均值池化输出形状(加 squeeze/reshape 去单例维);**注意该文件被存活 run_pt 进程 import,红线③:须在无训练窗口改**,或经 `git worktree` 途径;
  2. 修后用**现有 checkpoint 手动跑 t3_s0 的 LP**(参照 arb3_base 手动 LP 链先例),出数落 csv 后队列重排项会被 `lp_has_auprc` 自动跳过 → 省 2.7h;
  3. 若无法及时修:s0 重跑开跑前用 patrol-hold 类手段占位可避免白烧(巡检不代做)。

## ★ 利好:队尾变换任务干净跑通过初验

- `lead_swap02` s0 正常训练中(ep 27@22:40,~111s/ep),**未复现"秒败"** → 此前 speed_perturb/asym_view 的秒败确认为人为让位而非原生崩溃(与 20:00 更正一致),队尾任务可自动推进。

## 一、GPU 与车道

- 18,021/24,564 MiB(73%),利用率 95%;3 runner 存活(laneC 压 lead_swap02 / laneF 压 t3_s2 / laneG2 候 t3_s0 重跑);
- run_pt ×2 = lead_swap02_s0(pid 23172)+ t3_pow3_s2(pid 55440);claims 4 个均对应;存活日志零 OOM/Traceback;watchdog_relay 保持废弃。

## 二、在跑任务与 ETA

| 任务 | 进度 | 速度 | ETA |
|---|---|---|---|
| `lead_swap02` s0(laneC) | 27/200 | ~111s/ep | PT ~01:40;**判定 ~01:45** |
| `t3_pow3` s2(laneF) | 27/200 | ~118s/ep | PT ~04:30;**LP 预计崩(见上)**,修复及时则正常出数 |
| `t3_pow3` s0 重跑(laneG2) | 候显存(需 7800/空闲 6.1G) | — | ~01:45 开跑(**建议修复后改手动 LP 免重烧**) |

## 三、本小时采取的动作

纯巡检,无自愈写操作(无 runner 死亡)。

## 四、告警与待办

1. 【主会话·高优】t3_pow3 LP 形状 bug 修复时机 + s0 手动 LP 替代重跑(见上详述);
2. 【观察】h1_white8 上线时检查其 LP 是否正常(若池化/白化同样有形状路径问题,会同样崩);
3. 【git】本轮巡检提交与推送结果见巡检结论;main 分叉待空闲窗口;
4. 挂起不变:merge 远端 main / proj_dim4096 patrol-hold 等独占窗口 / 巡检去重闸。
