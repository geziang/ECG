# 主机B 队列 第二批 (HOSTS §四 P3: C3 准周期 MixUp + H4 HRV 借口, 2026-09-18 晨)
# 第一批(n3/h3/sinc)已全部完成判负, 见 matrix_results_hostB.csv
# anchor: auto = 本机 B0 锚点(csv b0 行 0.7158); Δ>0 自动补 seed2/4(对照臂 no_confirm)
# C3 纪律: 普通 MixUp 对照(--mixup-align 0)与对齐版同参数同机配对
# 启动: python pipeline_runner.py --lane B --queue runlog/M/pipeline_queue_hostB.yaml \
#     --results matrix_results_hostB.csv --max-pts 1 --vram-need 7000
anchor: auto
tasks:
  - {name: c3_align_mix02, type: probe, args: "--mixup-prob 0.5 --mixup-align 1", seeds: [0]}
  - {name: c3_plain_mix02, type: probe, args: "--mixup-prob 0.5 --mixup-align 0", no_confirm: true, seeds: [0]}
  - {name: h4_hrv01, type: probe, args: "--hrv-weight 0.1", seeds: [0]}
