# NFH A/B 清洗口径核对（08 任务书 §5 周一遗留项）

**日期**：2026-09-21 17:35（主机 A）
**性质**：CPU 侧核对，不占用 GPU；结论供主结果口径决策引用。

## 结论

**建议主结果统一采用主机 A 口径（`nfh-v1`，34,905 条全保留）**；主机 B 的 97 条 non-finite 剔除口径保留为敏感性分析，且需 B 侧先补交明细才能解释差异来源。

## A 口径事实（双重验证）

1. **制备时检查**（`prepare_nfh.py` L66-69）：z-score 归一化之前执行 `np.isfinite(x).all()` 与全平检查（`std < 1e-6`），34,905 条中 **read_fail=0 / bad_shape=0 / non_finite=0 / flat_signal=0**。
2. **独立全量复扫**（2026-09-21 17:33，本核对新增）：对 `data/pt_pretrain_nfh/samples/` 全部 34,905 个 `.npy`（归一化后、即实际训练输入）逐文件 `np.isfinite` 扫描，**non-finite = 0**，耗时 9 s。

即 A 侧从原始读取到训练输入端到端可验证无 NaN/Inf；训练日志损失连续有限与此一致。

3. **内容重复**：3 对 SHA256 重复（JS33139==JS33140、JS45461==JS45462、JS19114==JS19115）仅标记、未剔除——保留理由：占比 6/34,905 ≈ 0.017%，对 matched updates 与下游 split 的影响可忽略，删除反而破坏与 W1 冻结锚点（B0/C1/C2 seed0 均基于 34,905 训练）的可比性。
4. **无患者 ID**：以记录内容 SHA 查重替代患者级去重（NFH .hea 无患者 ID，已在 Limitations 素材中）。

## 与 B 口径的分歧及待办

08 任务书 §2.5 记载"主机 B 因 non-finite 排除 97 条"。A 侧实测零非有限值，两者矛盾，可能的来源（按可能性排序）：

1. B 的数据副本存在传输损坏或子集差异（B 侧文件级问题，非清洗标准分歧）；
2. B 的检测时机不同（如在原始 500 Hz 未滤波段执行，重采样/滤波边缘可产生 NaN）；
3. B 的判定阈值/实现差异。

**B 侧待办**：推送 `nfh_manifest`（含 `excluded_records` 明细中 97 条 record 名）至仓库，供逐条对账。对账完成前，B 口径数字不进入主结果。

## 对 confirm 矩阵的影响

`runlog/W2/confirm_matrix.py` 全部 PT 使用 `data/pt_pretrain_nfh`（A 口径制品），与 W1 冻结锚点同源同口径——C2 三种子 paired delta 的可比性不受影响。若未来采纳 B 口径做敏感性分析，需在 B 机以 B 制品重跑对应配置，不得混用两侧制品。
