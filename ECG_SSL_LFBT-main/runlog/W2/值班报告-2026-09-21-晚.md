# W2 值班报告（2026-09-21 晚 · 主机 A）

**值守范围**：08 任务书《论文收敛与下一阶段实验计划》确认矩阵启动后首轮巡检 + 周一遗留项闭环。
**执行方针**：日程仅作顺序参考，连续推进，验收只看结果与预注册门槛（08 文档 §0.1 主机A执行备注，提交 `a49e7d7`）。

## 一、结论

1. **环境健康，GPU 满载**：21.1/24.5 GB、93–94% 利用率、61–65 °C；三路确认预训练稳定运行，无卡死、无空窗风险。
2. **断点续训机制实战生效**：三路 `train_state.pth`（827 MB/路）每 epoch 原子落盘、时间戳新鲜，epoch 状态可读——重启事故损失上限从"整程"压缩到"1 epoch"。
3. **周一遗留项 NFH A/B 口径核对已闭环**（CPU 侧）：A 口径全量实测零非有限值，建议主结果采用 A 口径（详见 `runlog/W2/nfh_manifest_reconciliation.md`），B 侧需补交 97 条剔除明细。

## 二、确认矩阵运行状态（17:40 快照）

| 实例 | 进度 | 速率 | 预计完成 |
|---|---|---|---|
| c2_seed0（NFH 100ep, trc=1） | epoch 14/100 | ~2.9 分/epoch | ~21:45 |
| c1_seed0（NFH 100ep, trc=0） | epoch 14/100 | ~2.9 分/epoch | ~21:45 |
| b0_seed2（PTB 200ep） | epoch 25/200 | ~1.6 分/epoch | ~22:10 |

- 三路 scope 驱动（`confirm_matrix.py c2/c1/b0`）+ 三个 `run_pt.py` 子进程经 PID 直查确认存活；`sh()` 截留子进程 stdout，drv 日志滞后属预期，判活以 `train_state.pth` 时间戳/字节数为准。
- **自动接续链**（已核代码）：每驱动内串行 PT 全 seeds → CPSC LP → PTB FT10 → PTB LP → Chapman LP（血缘域最后）；账本 `runlog/W2/confirm_results.csv` 幂等去重、从 config.json 回填 git_sha/checkpoint_sha256。
- 全矩阵规模：C1/C2 × seeds{0,2,4} + B0 × seeds{2,4} 共 8 路 PT + 对应下游；B0 不做 CPSC/Chapman（§3.1 锚点范围）。

## 三、监测点（不干预，仅记录）

1. **相位重叠显存竞争**：稳态三 PT ≈ 21 GB；后续若出现 2 PT + 2 LP（LP 探针 ≈ 6.7 GB）叠加可能触顶 24 GB。驱动幂等，失败项重跑驱动即可补账，不会产生重复行；预计影响为分钟级延迟，非数据风险。
2. **ETA 口径**：当前速率含三路分抢；b0 驱动独占时段会自然加速，勿用即时速率线性外推全程。

## 四、NFH A/B 口径核对（周一遗留项，本次闭环）

- A 口径 `nfh-v1`：34,905 条全保留，制备时零剔除（isfinite+全平双检查）+ **本次独立全量复扫 34,905 个训练输入样本，non-finite = 0**（9 s）。
- 3 对 SHA256 内容重复仅标记未删（0.017%，保锚点可比性）。
- 与 08 任务书"B 侧剔 97 条 non-finite"矛盾 → 待 B 推送其 manifest 及 97 条 record 名对账；对账前 B 口径不进主结果。
- **对确认矩阵无影响**：confirm 全部 PT 用 A 口径制品，与 W1 冻结锚点（B0 `0.7177` / C1 `0.6574` / C2 seed0）同源。

## 五、下一步（自动推进，无需人工）

1. ~21:45 c2/c1 驱动接续 seed2 PT；~22:10 b0 驱动接续 s4 PT。
2. 各驱动 PT 全清后自动进入下游评估（CPSC LP 优先级最高）。
3. 种子纪律：CPSC LP 3/3 同号且均值 ≥ +0.5 pt 才保主结果；任一种子反向即判负；只读 val 选点，冻结后单次 test。

## 六、git

- 本报告与 `nfh_manifest_reconciliation.md` 随本次提交推送；远端基线 `a49e7d7`（含 08 文档主机A执行备注）。

---
*主机A 值班会话生成；数据来源：nvidia-smi / Get-CimInstance PID 直查 / train_state.pth epoch 读取 / prepare_nfh.py+全量复扫。*
