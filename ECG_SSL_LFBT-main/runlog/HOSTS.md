# 多主机并行实验协调表(HOSTS)

> **目的**:多台主机依托本仓库并行执行 M 筛选矩阵与判决实验。**本文档是唯一任务认领台账**:任何主机在启动任何任务前,必须 `git pull --rebase` 并查本文档,凡状态为 🏃(执行中)或 📋(已认领排队)的任务**一律不得重复启动**。
>
> **认领方式**:把对应任务行的状态改为 `🏃<主机名>(开始时间)` 并 commit+push;完成后改为 ✅,并把结果行写入本主机结果文件(§五-3)。
> **更新纪律**:任务状态每次变化(领取/完成/失败)立即 push;长任务不必频繁汇报,但必须写明预计完成时间。
> 文档路径:`runlog/HOSTS.md`;最后更新:**2026-09-17 20:10,办公机(流水线工作区)依用户指令完成 A/B 任务再分配**。
>
> **⚠️ 主机A当前状态:⏸ 暂停(19:42,用户指令)**——待按 §三 新队列恢复(恢复命令不变,见 §三 末尾)。
>
> **本轮再分配三原则(2026-09-17 晚,用户指令)**:①台账只按实际情况分配任务,**实现细节(开关/脚本/单测)由各主机自理**;②**总目标 = 尽快拿到至少一个过 3-seed 门的涨点、出成果**,判决线优先于扫描线;③已判死/无判值任务一律清除,机时不许再花。

---

## 一、主机登记表

| 主机 | 硬件 | 关键环境 | 当前状态 |
|---|---|---|---|
| **主机A = `Win4090`**(F:\新实验) | RTX 4090 24G / 32 逻辑核 / 128G RAM,双车道 | Win10,Python 3.10.11,torch 2.0.0+cu118,conda env `DL` | ⏸ 暂停(19:42);**待按 §三 新队列恢复** |
| **主机B = `DESKTOP-0PBLCND`**(E:\GZA) | RTX 3080 10G / 16 逻辑核 / 64G RAM,**单车道** | Win10,Python 3.11.9,torch 2.5.1+cu121;数据已校验(含 5 个损坏 .mat 修复);推送走 `ECG-push-tmp` | 🏃 B0 锚点运行中(19:03 起,约 4.5~7h);**任务已分配(§四)** |
| 主机C | (待登记) | (待登记) | 🆓 |

## 二、已砍任务清单(2026-09-17 晚裁定,⛔ 任何主机不得再启动)

| 任务 | 原位置 | 砍除理由 |
|---|---|---|
| `crop_weak` 的 FT10 评估 | chainB2 | 裁剪轴已判死(0.7 三 seed 门负),死臂 FT10 进不了任何结论;**只保留 B0 锚点的 FT10** |
| `D1L×AR-B3` 预训练+LP | chainB3 | D1L 在 B0 上经有效 NEG 验证判负(−0.60pt / NEG −0.96pt);E006"基座翻盘"先例属**对齐先验族**(P1/S1),D1L 属 projector 目标矩阵族(已知雷区),跨基座翻盘先验低;机时让给判决线 |
| 裁剪轴 / 掩码轴一切后续 | 全部 | crop 曲线五点闭合(0.4<0.5<0.7峰>0.8>0.85>0.9 全负);`aug_mask_weak` ❌(−0.60pt,掩码轴关闭)。**两轴永久关闭** |

## 三、主机A任务(判决线优先;恢复后自动按新队列执行)

**恢复方式(不变,幂等)**:`pipeline_runner.py --lane C`(显存允许可加开 `--lane D`)。**队列文件 `runlog/M/pipeline_queue.yaml` 已按本节重排**;勿再重启 chain*.sh(其内容已部分作废)。

### A-P1 判决战线(立即可跑,零新代码——目标:拿涨点)

| 序 | 任务 | 类型 | 说明 |
|---|---|---|---|
| 1 | `arb3_base` seed0 | ar_pt | E006 重确认的**配对基座** |
| 2 | `psfull_arb3` seed0 | e006_pt | **全项目最接近涨点的判决实验**:E006 full×AR-B3 在异机(3080)曾 LP +0.40pt 且 3/3 同号,按红线 1 回 A 单机重确认。**判读对 arb3_base 配对**;Δ≥+0.3pt 即补 seed2/4,3-seed 过门 → 启动 SD-LFBT 组合与论文主表 |
| 3 | `d7_rec01` / `d7_rec003` seed0 | probe | D7 重建支路;判读对 B0(B0 即纯 BT 对照);预期按 06 v2.5 从克制 |
| 4 | `common_view01` seed0 | probe | D3 共模前哨;**其结果同时裁决 C2(形态-节律解耦)立项与否** |
| 5 | `proj_dim1024` / `proj_dim4096` seed0 | probe | 投影容量轴(暂停中断,幂等重跑) |
| 6 | FT10 × B0 锚点 | 手动轻任务 | `run_ft.py` 一次调用(output_dim bug 已修);效率轴锚点数据,**crop_weak FT10 已砍勿跑** |

### A-P2 文献第一批(《06-新实验路线调研》v2;开关/脚本由 A 自行实现,通过"默认关==B0 逐位一致"单测后取消 pipeline_queue.yaml 内注释入队)

| 序 | 任务 | 闸门/纪律 |
|---|---|---|
| 1 | **H5 评测校准**(LP 特征标准化+正则小网格,不重训,~1–2h) | A-P2 最先;校准后**冻结新锚点写入 §五-2**,新旧口径禁止混用;所有 checkpoint 用同一套新尺 |
| 2 | **有效秩诊断**(B0+TFS 各一次 forward,<0.5h;口径与判据已冻结于 06 §v2.4) | 直接裁决 H1 立项与否 |
| 3 | **H2 BlurPool filt=3** ×3 seeds(≈6h) | 过 3-seed 门再加 filt=5 与随机核 NEG;附"时间平移等变探针"机制图 |
| 4 | **T3 Power-Mean Q=3** ×3 seeds(≈6h) | 过线再扫 Q∈{2,4};重点看 MI/HYP/STTC 与 FT10 |
| 5 | **H1 白化 g=8** ×3 seeds(仅当有效秩诊断证实塌缩) | 必带 LN-NEG 与位置-NEG;全程记录 RankMe 变化 |

### A-P3 队尾(有空位再跑)

`speed_perturb` / `asym_view` / `lead_swap02` / `cautious_adam` → `b0fast` → CPSC 跨库评价(用既有 checkpoint,先备 CPSC 数据)。

> **条件追加规则**:probe 类 seed0 Δ>0 由 runner 自动补 seed2/4(3-seed 符号门);**已关轴(裁剪/掩码)不受此规则保护,一律不得跑确认轮**。

## 四、主机B任务分配(已定;实现细节 B 自理)

**红线重申**:B 的一切 Δ 只对 **B 本机 B0 锚点**;结果只写 `runlog/M/matrix_results_hostB.csv`(不共写主 csv);推送走 ECG-push-tmp;任何方向**最终判定实验回主机A 单机重跑**。

| 优先 | 任务 | 说明 |
|---|---|---|
| P0 | 入场 B0 锚点(🏃 19:03 起) | 完成后锚点入 hostB csv,作为 B 一切 Δ 的分母;绝对值与 0.7177 差 0.8~1.0pt 属跨 GPU 正常波动,方向与 Δ 才可比 |
| P1 | `n3_prob03` seed0(**代码已就绪**:`--n3-prob 0.3`) | 患者轴三选一的"**拉近**"臂 |
| P1 | **H3 身份不变性轻量臂** seed0(B 自实现;建议轻量去相关版优先,对抗版缓行;附 patient-id 线性探针可解码度诊断) | "**去除**"臂;与 n3 **同机同协议配对**,三选一(拉近/去除/不动=B0 锚点)一次读数定方向,避免 N3 做反;任一 Δ>0 → B 补 seed2/4 |
| P2 | **C1 Sinc 参数化前端** seed0(B 自实现;M=16/32 带通核,截止频率约束 0.5–40Hz,防退化为普通卷积的频带正则) | 06 第二批最高优先;Δ>0 升 3-seed;记录学到截止频率直方图(可解释性图) |
| P3 | C3 准周期 MixUp、H4 HRV pretext(均 B 自实现) | 排 C1 之后;C3 必须带"普通 MixUp"对照 |

### 新方法登记表(防多主机撞题;领取时改 🏃 并 push)

| 方法名 | 主机 | 一句话设计 | 状态 | 结果文件 |
|---|---|---|---|---|
| H3 患者身份不变性(轻量去相关) | 主机B | 同患者表征协方差 off-block 压零/负对 margin,与 N3 三选一 | 📋 已分配 | matrix_results_hostB.csv |
| C1 Sinc 带通前端 | 主机B | 第一层 Conv1d 换参数化带通滤波器组 | 📋 已分配 | matrix_results_hostB.csv |
| C3 准周期 MixUp | 主机B | 相位对齐后小比例混合,与普通 MixUp 对照 | 📋 排队 | matrix_results_hostB.csv |
| H4 HRV 信号内 pretext | 主机B | xqrs 离线 R 峰→HRV 统计量回归辅助头 | 📋 排队 | matrix_results_hostB.csv |
| H2 BlurPool-1D / T3 Power-Mean / H1 白化 / H5 校准 / 秩诊断 | 主机A | 06 v2 第一批,见 §三 A-P2 | 📋 待开关实现 | matrix_results.csv |

**触发链(全局,自动激活下一棒)**:`common_view01`(A-P1)出数 → 裁决 C2;`H1` 过 3-seed 门 → T1 SIGReg 作软正则对照;`cautious_adam` 出数 → 议 T2 Muon;T4/C4/C5/T5 不做(撞车/失败族,见 06 §5.2,看到相关 idea 直接判重)。

## 五、多主机方法学红线(必读,违反=数据作废)

1. **禁止跨机拼表**:同一模型的绝对 AUPRC 在不同 GPU 上可差 0.8~1.0pt(历史实测)。每台新主机**必须先跑本机 B0 锚点**(预训练+LP),本机全部 Δ 一律对本机锚点计算;跨机只比较方向与符号,最终判定实验回单机重跑确认。
2. **协议冻结**:预训练 200ep / batch 128 / Adam 1e-3 / RRC-TO(0.5,1.0,0.0,0.5)/ PTB-XL folds 1–8(17,418 条);下游 LP(冻结+线性,1e-3)与 FT(全量,1e-4)各 100ep,val 选 checkpoint、锁定 test 仅评一次。改动任何一项即不可比。**(H5 校准落地后,新锚点数值在此追加冻结并注明生效时间;历史行不重跑、不混算。)**
3. **结果文件分主机**:各写 `runlog/M/matrix_results_<主机名>.csv`(schema 同 matrix_results.csv),**不要共写** `matrix_results.csv`;主机A的历史行已在 matrix_results.csv。汇总合并由主机A定期执行。
4. **checkpoint 不入 git**(体积过大);结果行的 args 列已含完整复现命令。
5. **显存安全**:`run_pt/run_lp/run_ft` 已内置启动自闸门(6500/2500MiB,CUDA 初始化前排队),多车道并发不会 OOM;主机A 可开 2~3 车道,主机B(10G)**仅单车道**(runner 已按 7000MiB/并发1 适配)。
6. **数据准备**:`preprocess_ptbxl.py` + `prepare_data.py` 生成 `data/pt_pretrain`(17,418)与 `data/ptbxl`(train/val/test = 13,639/1,714/1,739);以 `data/manifest.json` 与 B0 checkpoint SHA256(`026033d5…`)核对一致性。
7. **(新)轴关闭禁令**:某轴经门判死(3-seed 门或曲线闭合)后,同轴任何任务**一律不得再启动,包括一切脚本/队列的自动接力与确认轮**;09-17 的 `aug_crop_085` 误启(预注册条件"0.8 也正才补缝"未满足但 baked 脚本无检查照跑)即为本条来历。
8. **(新)预注册条件必须脚本显式检查**:凡"若 X 正则跑 Y"类条件任务,入队前必须有脚本内条件判断;无检查逻辑的条件任务不得入队。

## 六、已完成任务速览(✅ 勿重复)

截至 09-17 19:00,`matrix_results.csv` 共 15 行判定(1 正 14 负,唯一"首正"crop_weak 已被 3-seed 门证伪为噪声):**d1l_05_02 ❌ / d1l_neg(负对照有效)/ n4_ema999 ❌ / aug_crop_strong(0.4)❌ / b0_gamma07 ❌ / b0_gamma09 ❌ / b0_lambd003 ❌ / b0_lambd01 ❌ / aug_crop_weak(3-seed 门判负)/ aug_crop_080 ❌ / aug_crop_090 ❌ / aug_mask_weak ❌(掩码轴关闭)/ aug_crop_085 ❌(裁剪曲线闭合)**。另:N1 四库语料 ❌、D9 VICReg 六形态 ❌(详见根目录实验报告)。

---

*维护责任:主机A。其他主机修改本表后请在行末注明自己的主机名与时间。*

> 主机B(DESKTOP-0PBLCND)09-17 19:10 登记入场;环境详情见 `runlog/HOSTB_环境报告.md`。本轮 A/B 再分配由办公机(流水线工作区)依用户指令于 09-17 20:10 更新并推送。
