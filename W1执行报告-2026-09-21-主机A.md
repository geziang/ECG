# W1 任务书执行报告(主机A)— 2026-09-21

> 依据:《报告归档/历史文档/07-下周科研与代码任务书-2026-09-19.md》;主机A = Win4090(RTX 4090 24G, torch 2.0.0+cu118)。
> 本报告覆盖主机A承担的 A 线(粗筛+终判)、C 线(全部)、T4/T5 交付;B 线归主机B(其机器在跑,见其台账)。
> 统计纪律:B0 锚点 0.7177 冻结;粗筛为 test 口径粗探(遗留管道),W1 门规按任务书 §八执行;所有跨域读数为单次评估。

## 一、总览

| 主线 | 状态 | 一句话结论 |
|---|---|---|
| A 解剖区域关系 | **关闭** | 三探针全灭(a1 −7.06 / a2 −9.11 / a3 −1.83pt),D1L“翻案”假设在当前 LFBT/PTB 协议下未成立；关闭当前实现，不外推为机制普遍有害 |
| B 心动周期重建 | 主机B执行中 | (B1/B2/B3/A4 在 3080 上跑,不属本报告) |
| C 外部 NFH 锚点 | **单 seed 参考/待冻结** | C1 锚点 0.6574(−6.03pt 同域参考);跨库表+37条件缺导曲线+FT10 已落袋；C2/TRC 仍需 paired 3-seed 和账本复核 |

## 二、A 线终判(关闭)

| 探针 | 配置 | AUPRC | Δ(vs 0.7177) | 判定 |
|---|---|---|---|---|
| a1_d1l_fix | `--d1l 0.5,0.2`(τ 对角修正语义) | 0.6471 | −7.06pt | ❌ |
| a2_acl_intra | 四区仅区域内 InfoNCE,τ=0.1 | 0.6266 | −9.11pt | ❌ |
| a3_acl_inter | 四区+跨区 Eq.(11),γ=0.5,τ=0.1 | 0.6994 | −1.83pt | ❌ |

- **核心科学结论**:修正语义后的 D1L(τ 入对角、PSD 矩阵)比旧错误实现(−0.60pt)更差——“旧负结果纯属实现错误”的翻案假设在当前协议下未成立；该结果只支持关闭当前 D1L/ACL 实现，不支持对结构化导联关系目标作普遍否定。
- 结构信号:a3 比 a2 高 7.3pt(跨区 Eq.11 项大幅回血),方向与 ACL-ECG Table 11 消融(仅 intra 0.7313 < 双路 0.7421)同向;但绝对值不过线。ACL-ECG 增益依赖其自身架构+NFH 语料,迁移到 LFBT+PTB 预训练不复现。
- 按 §八止损(1-seed < −0.5pt 即止):不进 3-seed、不跑 A4 随机分组 NEG。
- 实现备注:T1/T2 双机各实现一版,终态取主机B论文核对版(τ=0.1 默认,acl_paper_check.md 依据用户提供的 PDF);单测闭合基线(--d1l-full / τ 语义)与 B0 逐位一致。

## 三、C 线(完成)

### 3.1 C1 外部锚点(T4 数据 + matched-updates 训练，单 seed 参考)

- NFH 预处理:34,905/34,905 全通过(零排除;3 条内容级 SHA256 重复记录在案),manifest+全库哈希入库 runlog/W1/;
- 口径差异记录:主机B同数据得 34,808(排除 97 non-finite;其源拷贝/检查位相异),差异 0.28%,双方 manifest 均在库;
- matched-updates:NFH 100ep(27,270 步)对齐 PTB B0 200ep(27,216 步),差 0.2%;
- **C1 = 0.6574 / −6.03pt**(对 PTB 同域 B0 的跨域参考,非同域门)。

### 3.2 C3 跨库迁移表(LP,单次评估，未冻结主表)

| 预训练 → 下游 | PTB-XL(5类) | CPSC(9类) | Chapman(4类) |
|---|---|---|---|
| B0(PTB 同域) | 0.7177 | **0.8105** | 0.9796 |
| C1(NFH 外部) | 0.6574 | 0.7857 | **0.9891** |
| C2(C1+TRC) | 0.6564 | **0.8092** | 0.9889 |

- **血缘效应**:NFH 与 Chapman 同团队同设备(JS 连号发布),C1 在 Chapman 反超同域预训练 B0 +0.95pt;CPSC(第三域)上 PTB 预训练更优。matched-updates 排除了训练量混杂 → 差异来自数据血缘本身(§十 Q3 作答)。
- **FT10(10% 标签微调,PTB)**:B0 = 0.8756/0.6433(历史冻结数);**C1 = 0.8604/0.6341(Δ−0.92pt)**——微调把 LP 口径的 −6.03pt 域差收窄到 −0.92pt,外部语料+10%标签≈同域预训练(标签效率结论)。

### 3.3 缺导鲁棒性(37 条件 × 2 权重,导联置零口径)

- 最敏感:II+III 同缺(B0 −6.13pt / C1 −3.64pt)——肢体基底导联承载主要信息;最不敏感:V5/V6(≤0.06pt);
- 平均单导相对完整记录掉幅:C1 约 −0.0128 < B0 约 −0.0165（AUPRC 原始单位）——**外部预训练表征相对更稳,但绝对性能 0/37 反超**;
- 完整曲线 runlog/W1/missing_lead_curves.csv(74 行)。

## 四、协议差异与事故记录(诚实清单)

1. batch 128 vs ACL-ECG 论文 64(本机冻结协议,红线);8 导联子集 vs 论文 12 导联;
2. 粗筛读数为 test 口径(遗留管道),按任务书本应 val——A 线全灭使该偏差不影响任何门判定(无候选过门);T0 val 纪律字段已随主机B合并入代码;
3. 09-20 23 时整机重启:a1/a2/a3 半程全损(run_pt 无断点续训),02:44 恢复重跑——所有报数来自重启后完整运行;
4. fast-backbone 等价性检验(b0fast −1.22pt):单算子数值等价≠整训等价,fast 路径不得用于协议跑;
5. τ 手术记录:a2/a3 首跑 τ=0.5(epoch<25)被击杀,终值 τ=0.1(论文 §4.2,主机B PDF 核对);
6. NFH 97 条口径差异(见 3.1)。

## 五、run registry(T5 摘要)

| run | git | seed | 数据 | 读数 |
|---|---|---|---|---|
| B0 锚点 | 历史冻结 | 0 | ptbxl folds1-8 预训练 | LP 0.7177 / FT10 0.6433 |
| C1 nfh_b0 | 940bced 前 7fefdb6 | 0 | pt_pretrain_nfh 34,905 | PTB 0.6574 / CPSC 0.7857 / Chapman 0.9891 |
| C2 nfh_trc | 14c4d29 | 0 | 同上 + --trc 1 | PTB 0.6564/CPSC 0.8092/Chapman 0.9889 |
| C1 ft10 | dacde53 后 | 0 | ptbxl 10% 标签 | 0.8604/0.6341(Δ−0.92pt) |
| a1/a2/a3 | 940bced 合并后 | 0 | pt_pretrain | 见 §二 |
| 缺导扫描 | dacde53 | 0 | ptbxl + zero-leads | 74 行 csv |

checkpoint:checkpoint/ptxl_gamma08(B0)、checkpoint/M/c1_nfh_b0_seed0、checkpoint/M/c2_nfh_trc_seed0;全部含 encoder_group.pth + SHA256 落盘行。

## 六、§十 三问作答(主机A部分)

1. **A 线收益来自解剖关系还是容量?** 当前三探针均未过门，因此关闭该实现和协议；不能由此推出所有解剖区域目标都无效。
2. **B 线?** 归主机B。
3. **C 线差异来自外部数据、暴露次数还是结构?** matched-updates(±0.2%)排除暴露次数;结构未变;**差异来自预训练语料与下游的血缘**(Chapman 反超/CPSC 不及的双重模式)。

## 七、结论分类

- **当前协议下确认/关闭**:D1L/ACL 实现未迁移并关闭;C 线出现数据血缘效应;C1 缺导相对更稳但绝对值低;fast 路径不可替代协议跑。
- **趋势**:a3>a2 的跨区项回血(单 seed,仅方向性)。
- **趋势(建议 3-seed 确认)**:C2/TRC 在 CPSC +2.35pt 定向补跨域短板;FT10(C1)−0.92pt(微调收窄域差);a3>a2 跨区项回血。
- **关闭**:A 线当前实现;旧矩阵 38 行全轴。B1/B2/B3 仍为已实现/待验证，不归入失败。

## 八、遗留与建议

- run_pt 断点续训(重启事故的直接教训,长探针质变级改进);
- T0 val-only 筛选纪律待在下一次筛选活动前落地(A 线已死故本周无影响);
- 主机B版 run_pt 缺 A-P2 backbone kwargs(轴已关,惰性丢失,已在案);
- 建议双机下一批候选从 B 线(主机B)与 C 线扩展(缺导场景专用适配)产生。

---
*主机A 值班会话生成;数据文件:matrix_results.csv(42 行)/ c3_evals.csv / missing_lead_curves.csv(74 行)/ runlog/W1/*。git: 14c4d29 及此前全部推送 GitHub main。*

---

## 教授复核附录（2026-09-21，供论文写作引用）

本节不修改上文的原始运行记录，只更正结论的证据等级和论文措辞。

1. **A 线是“当前协议关闭”，不是对机制的完全证伪。** D1L-fix、ACL-intra 和 ACL-full 的负结果可以支持停止当前实现和超参，但不能直接写成“结构化导联关系目标真实有害”或“ACL-ECG 被证伪”。当前 `run_pt.py` 将 ACL 项以 `L_LFBT + L_ACL` 叠加，尚非严格替换；同时 batch、预训练语料、损失归一化和下游协议与 ACL-ECG 不同。a3 相对 a2 回升 7.30 pt，只能作为跨区域正对具有机制作用的方向性证据。

2. **C2 的 CPSC 读数是单种子趋势。** C1 为 0.7857，C2 为 0.8092，因此 C2-C1 是 +2.35 pt；同域 B0 为 0.8105，C2-B0 是 -0.13 pt。正式论文必须等 seed 0/2/4 的 paired confirmation 后再决定是否把 TRC 写成主方法。

3. **缺导结果是相对鲁棒性，不是绝对性能提升。** C1 在 37 个导联置零条件下没有反超 B0；“平均下降较小”不能改写为“缺导性能更高”。

4. **B 线尚未完成。** 主机 B 队列中的 B1/B2/B3、随机 ACL 和 D1L 负对照尚无最新结果入库；在论文中应标记为“已实现/待验证”，不能写成正向或负向结论。

5. **W1 A 线候选使用了遗留 test 粗筛。** 因候选全部明显负，关线决定没有被改变，但下一轮只能使用 val 选点，冻结后再评 test 一次。

6. **C2 评估账本需要返工。** `runlog/W1/c3_evals.csv` 的 C2 表头已切换到 `ft10_auroc/ft10_auprc`，但 `runlog/W1/c2_chain.py` 的 `lp_eval()` 调用的是 `run_lp.py`。因此 C2 的跨库数字在 LP/FT10 标签上尚未冻结，论文主表必须在修正脚本后重新评估，当前只能保留为未冻结的单种子读数。
