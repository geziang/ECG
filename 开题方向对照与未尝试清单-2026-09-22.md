# 开题方向对照与未尝试清单

> 对照日期：2026-09-22
> 用途：把开题报告中的研究方向映射到当前代码、结果和可用资源，明确哪些内容可以写进当前论文，哪些只能作为待验证线，哪些应留到后续课题。
> 重要口径：**代码存在不等于实验完成；单 seed 趋势不等于稳定结论；8 导联实验不等于 12 导联验证。**

## 一、先给判断

开题报告与当前项目是同一大方向，但覆盖程度并不对称：

1. **已经形成证据主线**：LFBT 多导联自监督、下游心律失常分类、NFH 外部预训练、TRC/GRN1D 候选、缺导相对鲁棒性。
2. **已有代码和实验入口，但结果没有闭合**：心拍周期感知遮挡/重建（B1/B2/B3 CCM）、更完整的导联关系先验、噪声鲁棒性和 FT20/FT40 标签效率。
3. **基本没有开始**：原始导联相关矩阵到编码空间关系的软匹配、去噪/完整导联恢复、cognitive-stress ECG、多模态睡眠、部署压缩和系统可解释性。
4. **当前不应扩张**：在 C1/C2 三 seed 确认完成前，不启动新的大模块扫描；多模态睡眠不混入当前论文主线。

## 二、方向对照表

| 开题方向 | 当前代码/资源证据 | 已有结果 | 状态 | 当前处理建议 |
|---|---|---|---|---|
| 多导联 ECG 自监督预训练 | `run_pt.py`、`run_lp.py`、`run_ft.py`；8 导联 `[II, III, V1–V6]`；LFBT intra/inter BT | B0 LP AUPRC 0.7177，FT10 0.6433；相对随机初始化有效 | **已完成/主线** | 冻结为基座，不再做随机小模块扫描 |
| 心律失常分类 | PTB-XL 5 类、CPSC 9 类、Chapman 4 类下游管线；LP/FT 评估 | 已有 B0/C1/C2 单 seed 参考；C2 仍待 3-seed | **已完成但主结论待冻结** | 先完成 C1/C2 paired confirmation；补 FT20/FT40 和少数类指标 |
| 外部 NFH 预训练/跨库迁移 | `prepare_nfh.py`；NFH 34,905 条通过；CPSC/Chapman manifest | C1/C2 已有单 seed 跨库读数；出现数据血缘效应 | **部分完成** | 只写“跨库迁移/协议效应”，不写严格跨医院泛化；完成 seed 0/2/4 |
| TRC/GRN1D 轻量时序适配 | `models/vgg_1d.py`；每导联零初始化、总增约 1024 参数 | 历史同域有正向趋势；NFH C2 CPSC +2.35 pt 仅 seed 0 | **待验证/最高优先级** | C1/C2：CPSC LP → PTB FT10 → 关键缺导 → Chapman；按预注册门槛判定 |
| 心拍周期先验 | `runlog/prep_rpeaks.py`、`data_utils/ccm.py`；R 峰缓存 17,418 条成功率约 1.0 | B1/B2/B3 没有正式结果；HRV 辅助 probe 为负 | **部分完成/待验证** | C2 冻结后只做 B2 多段 vs B3 CCM 最小矩阵；不把 HRV 负结果外推到全部 RR 先验 |
| 整段-心拍双视角一致性 | 当前只有整段 RRC-TimeOut；无整段/心拍双编码视图结果 | 未尝试 | **未尝试** | 暂不做；先验证 CCM 是否有机制信号 |
| RR 间期预测、心拍内形态/心拍间节律联合 | 有 R 峰缓存和 HRV 预处理，但无 RR 预测头、节律联合损失 | HRV 头 `h4_hrv01` Δ约 −0.26 pt | **未尝试/低优先级** | 若 CCM 通过，再单独设计轻量 RR 辅助任务；不得与 CCM/TRC 同时首跑 |
| 导联空间关系保持 | 已有 LFBT inter-lead BT、D1L、ACL、LGA、`physiospatial.py` | D1L/ACL/LGA 当前协议下低于 B0；不能推广为理论否证 | **部分完成** | 暂缓 raw-correlation matching；先完成 C2 判决 |
| 原始导联相关矩阵→编码相似矩阵软匹配 | 当前没有 raw waveform correlation 与 feature Gram matching 实现 | 未尝试 | **未尝试/候选新轴** | C2 后做单模块 pilot：归一化 8×8 相关矩阵、低权重 Frobenius/秩匹配、缺导动态重算；5 epoch smoke 起步 |
| 导联缺失鲁棒性 | `missing_lead_sweep.py`；37 条件导联置零；`AdjacentLeadSwap` 训练增强 | B0/C1 缺导曲线已有；C1 相对掉幅较小但绝对未反超；`lead_swap02` 单 seed 为负 | **部分完成** | 先补 C2 关键单/双导；再做 test-time lead-swap 矩阵，明确区别“置零”和“物理错位” |
| 噪声鲁棒性 | 当前没有统一噪声注入器或 SNR 评估结果 | 基线漂移、肌电、工频尚无结果 | **未尝试/低成本** | 不训练新模型，直接对冻结 checkpoint 做 SNR 0/5/10/20 dB 的 baseline-wander、EMG、50/60 Hz 评估 |
| 少标签学习 | PTB FT10 已有冻结结果 | FT20/FT40、per-class AP、Sensitivity/Specificity、calibration 未形成表 | **部分完成** | 复用冻结 encoder 补 FT20/FT40；至少输出 AUPRC、Macro-F1、per-class AP、Sensitivity/Specificity |
| 去噪/信号恢复 | 只有 masked reconstruction decoder；旧 D7 单段重建有负探针 | `d7_rec003/d7_rec01` 为旧实现负结果；B2/B3 CCM 无结果 | **部分完成/待验证** | 将旧 D7 与 B1/B2/B3 分开；不做完整去噪论文线，先看 CCM 最小实验 |
| 部分导联恢复完整 12 导联 | 当前输入契约固定为 8 导联；无 12 导联目标、恢复指标或 decoder 评估 | 未尝试 | **未尝试/后续课题** | 不进入当前论文；需要新数据契约和 SNR/PRD/相关系数指标 |
| 质量感知动态融合 | 有 `valid_mask`/lead mask 基础，但没有连续质量分数、质量估计头和动态权重实验 | 未形成正式结果 | **未尝试** | 暂不新增融合模块；可作为导联质量退化评估的后续扩展 |
| cognitive-stress ECG | 当前无数据、manifest、标签或代码 | 未尝试 | **未开始** | 暂不投入；先完成已有公开数据主线 |
| 多模态睡眠检测 | 仓库无呼吸、血氧、EEG 数据/同步/标签管线 | 未尝试 | **后续课题** | 不混入当前 ECG SSL 论文；另立数据和任务计划 |
| 多标签 ECG | CPSC 有 `utils/multilabel.py` 指标工具，但当前预处理主流程是优先级单标签映射 | 工具存在，正式多标签主表未冻结 | **部分完成** | 先核对标签语义；若写多标签，必须单独使用连续概率 macro-AUROC/AUPRC 和验证集阈值 |
| 跨设备/域适配 | 已有 NFH→PTB/CPSC/Chapman 换语料评估；无显式 domain-adaptation 方法 | 观察到域/数据血缘效应；无适配算法结果 | **部分完成** | 当前写成“跨域迁移观察”；域适配另列后续，不把 TRC 等同于完整 DA |
| 可解释性 | 有 ERank/诊断探针和缺导曲线 | 无系统导联关系、心拍 saliency 图 | **未尝试** | 先做结果冻结后的低成本可视化，不作为主方法贡献 |
| 可穿戴部署/压缩 | 有 fast 等价性探针；无量化、延迟、能耗 benchmark | `b0fast` 不能替代标准协议 | **未尝试** | 当前不投入；待模型和任务冻结后再测 |

## 三、开题方向对当前实验最有价值的启发

### 1. 把“生理先验”具体化，而不是继续堆模块

目前最接近开题报告、又能复用现有资源的机制是 **CCM 周期内遮挡 + 轻量重建**。它能直接检验“不要横切 R 峰是否有助于学习局部形态”。但该线必须与旧 D7 单段 50% 重建分开记录：旧 D7 有负结果，B1/B2/B3 是新协议，尚未验证。

### 2. 导联空间关系要从“硬拉近”改成“结构保持”

历史 D1L/ACL/LGA 的共同教训不是“导联关系不能用”，而是直接把跨导联特征硬拉近、逐维对齐或改变 inter-BT 目标容易损伤导联特异性。开题报告提出的 raw-correlation → feature-similarity 软匹配，机制上不同，值得保留为 C2 后的单模块 pilot；不要与 TRC、CCM 同时叠加。

### 3. 鲁棒性优先用现有 checkpoint 做“评估基准”

开题报告的噪声、少标签、缺导问题不一定需要立刻重训。先对冻结 B0/C1/C2 做输入退化评估，可以低成本判断瓶颈到底是表征还是训练目标。建议顺序：

```text
C1/C2 三 seed冻结 → SNR 噪声曲线 → lead quality 连续退化 → 真实 lead swap → FT20/FT40
```

### 4. 当前项目的论文边界必须写清楚

- 当前是 **8 导联**，不是完整 12 导联；
- 当前 CPSC/Chapman 是 **record-level 70/10/20**，不能写成患者级跨医院泛化；
- NFH 没有患者 ID，只能做内容 SHA 查重；
- TRC/C2 当前外部结果是 **单 seed 趋势**，不是稳定提升；
- “LGBT”按上下文应改为 **LFBT**。

## 四、推荐的执行顺序

### P0：先完成已有主线

1. 核对 C2 跨库账本和 checkpoint/config SHA；
2. 完成 C1/C2 seed 0/2/4 paired confirmation；
3. 按 CPSC LP → PTB FT10 → 关键缺导 → Chapman 顺序冻结；
4. 按门槛决定 TRC 是方法主线还是协议/实证结果。

### P1：不训练新模型的低成本补强

1. 对冻结 checkpoint 做 baseline-wander、EMG、工频 SNR 曲线；
2. 做 C2 关键 lead-swap 评估；
3. 补 FT20/FT40 和少数类/校准指标；
4. 形成一张“干净性能—噪声—缺导—标签效率”统一鲁棒基准表。

### P2：只保留一个机制新实验

如果 C2 通过：优先结束新增机制，转入写作。
如果 C2 不通过：做 B2/B3 CCM 最小实验（5 epoch smoke → 单 seed full），作为唯一新增机制线。
无论哪种情况，raw-correlation 软匹配都推迟到主线判决后。

### P3：后续课题，不进入当前论文

- 12 导联重建/导联恢复；
- cognitive-stress ECG；
- ECG+呼吸/血氧/EEG 睡眠检测；
- 显式域适配、可穿戴部署与模型压缩。

## 五、验收与归档规则

每个新实验必须同时写入：

1. 研究假设和与 B0/C1/C2 的 paired 对照；
2. 数据、导联、split、seed、git/checkpoint SHA；
3. val 选点规则和只评一次的 frozen test；
4. 失败也要写清止损原因，不再散落在临时报告中。

最终报告只保留四张核心表：

- B0/C1/C2 主结果表；
- paired delta/seed 符号表；
- 噪声—缺导—少标签鲁棒性表；
- 失败与关闭方向总表。
