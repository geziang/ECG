# 06 · 外部路线对接与下一步修订建议（2026-09-18）

> 上游：《01 主线设计》《02 模块调研》《03 数据与新轴》《04 工程适配》《05 资产索引》《LFBT 基线改进实验报告 2026-09-17 完善版》
> 输入：外部 2024–2026 年 30+ 篇文献调研（含 LFBT 同团队续作 ACL-ECG 全文）+ 本仓库全部代码静态审查 + 截至 09-18 19:29 双机台账
> 性质：在**内部九轮负结果墙**之上做增量修订，不推翻既定纪律（1-seed 粗探 → 3-seed 符号门 → 配对检验）；所有新候选均标注"与哪条负结果同族/不被哪条覆盖"

---

## 〇、四个情报增量（先看这个）

| # | 情报 | 性质 | 影响 |
|---|---|---|---|
| Q1 | **D1L 的代码实现把目标值 τ 放错了矩阵位置**（改的是 off-diagonal，应为 on-diagonal），且设计前提误读了 LFBT inter 损失（inter 对角目标本来就是 1，不是"全 0"） | 代码级机制问题，非方向证伪 | D1L 插槽**应翻案重测**，改动量 <10 行，优先级 P0 |
| Q2 | **LFBT 同团队续作 ACL-ECG 已发表**：解剖 4 区域 + 区域内一致/区域间 InfoNCE 判别 + 生理增强（CCM 周期掩码 + TS 时移），PTB-XL LP AUROC 0.9145/AUPRC 0.7145，5–8× 标签节省 | 外部已验证的"完整版 D1 插槽" | 我们的 LGA 只做了"组内 BT 对齐"，**缺区域级 projector 和组间判别臂**；LGA 负 ≠ ACL 式方法负 |
| Q3 | **NFH 与 Chapman 均为公开数据，09-15"判私有弃用"是误判**：NFH = PhysioNet Challenge-2021 `ningbo`，**34,905 条**公开；Chapman/Shaoxing = figshare 4560497，**10,646 条**公开（亦在 PhysioNet `ecg-arrhythmia`） | 数据可得性纠正 | 可补"论文同源外部库预训练"锚点 B0′，消除"PTB-XL 既预训练又下游"的最大协议短板；Chapman 补第三个跨库 |
| Q4 | **D7（掩码重建）结果尚未归账**（09-16 晚 d7_rec01/003 启动后被队列切换，两份 csv 无 d7 行）；而 2025–26 外部主流（ECG-FM、KDD25-HL、ECG-LFM、CoRe-ECG）全部收敛到"对比+重建"混合目标 | 结果待回收 + 外部共识最强 | 先回收结果；若负，按 §四 的预案诊断 mask 策略，不要直接归档 |

---

## 一、内部证据墙 × 外部证据交叉判定（截至 2026-09-18）

### 1.1 内部全部结果与外部对照

| 内部实验 | 结果（Δ AUPRC） | 外部对应证据 | 修订判定 |
|---|---|---|---|
| B0 复现（PTB-XL 17k 预训练） | LP 0.7177（论文 0.6923） | LFBT 原协议用 NFH 44k | 锚点成立但**语料不同源**，需 B0′（Q3） |
| E002 注意力/ECA/融合权重 | −0.6 ~ −2.1pt | NEDC 在 CODE 200 万条上发现通道增加反降、派生导联无益 | **双证关闭**，不再碰重加权 |
| E003 Mixer/GAT 导联通信 | 明显退化 | Graph-CMMC 的 GNN 服务于 GADF 伪多模态，非纯 SSL 增益 | **关闭**（8 节点图上消息传递内外都不支持） |
| E004/E007 TRC（GRN1D，+1024 参数） | FT10 +1.1pt、缺导 +2.8~3.1pt，3/3 | TolerantECG（ACM MM 2025）证明缺导鲁棒是热点卖点 | ✅ **LRTC-Net 主成果**，外部趋势加持 |
| E005 跨导掩码蒸馏 | −2.3pt | — | 证伪关闭（重建式 D7 不同族，不受影响） |
| E006-S1 组内 BT 对齐 | B0 无增益；AR-B3 上 +0.40pt(3/3) | **ACL-ECG：组内一致+组间判别才完整** | S1 只做了一半 → 被 Q2 的 D1-ACL 取代升级 |
| E006-P1 时频对齐 | 弱正、基座依赖 | D5 同类，外部增益一般 | 维持第二梯队 |
| E007 LGA 组内拉近（η=0.05） | FT100 **−0.89pt 显著负** | ACL 用区域 projector + 组间判别 + γ=0.5，不叠加在原 inter 项上 | LGA 实现 ≠ ACL；**不关闭插槽，按 Q2 重开** |
| D1L 结构化目标（0.5,0.2） | −0.60pt（AUROC 反超 +0.22），NEG −0.96 | ACL 解剖分组消融：正确分组比随机分组高 1.0/3.5pt | **实现位置错误（Q1）→ 翻案重测 D1L-fix** |
| D9 VICReg 六形态 | −1.3 ~ −10.7pt | VICReg 外部 batch 鲁棒主张未在本协议复现 | 关闭（本协议下 BT+BN 已是局部最优） |
| N1 五库乱炖 38.7k | **−3.07pt** | ECG-FM/PCLR 均为**单体系大规模**，非多库乱炖 | 关闭"乱炖"；改"单院外部库"路线（ningbo/CODE） |
| N3 患者正对（拉近/去除） | −0.97 / −14.98pt | PCLR（320 万）患者级对比成立但需大规模复诊样本 | PTB-XL 复诊占比 ~15% 撑不起，关闭 |
| N4 EMA 0.999 | −2.68pt | — | 关闭 |
| C1 Sinc 前端 | −2.71pt | — | 关闭 |
| C3 MixUp（对齐/朴素） | −1.69 / −1.44pt | — | 关闭 |
| H4 HRV 辅助头 | −0.26pt | — | 关闭 |
| γ/λ 双侧扰动 | 双侧下坡 | — | 0.8/0.0051 确认局部最优 |
| 裁剪 0.7 弱裁剪 | seed0 +0.27，seed2/4 转负 | **ACL 采用 RRC 0.8–1.0 弱裁剪**，方向一致 | 信号同向但在噪声内；随 D1-ACL 的增强套件一并定版 |
| D7 时间掩码重建 | **结果未归账** | ECG-FM/KDD25/ECG-LFM/CoRe-ECG/MaeFE/MAE-ECG 一致正 | 回收结果 + 预案（§四） |
| D3 共源-特异解耦 | 未跑（common_view 雏形已写、未出数） | LVCG（2026，VCG 潜空间去冗余）同向 | 方法学主创新位，保留 P1 |
| D8 物理噪声 + N7 纯 LeadDropout | 未跑（B3 的 LeadMask 与 mean/FusionBT 混测，非干净测试） | TolerantECG、ACL 电极置换 | P1/P2，与 TRC 缺导卖点协同 |
| D4 Mamba 全局主干 | 未跑 | ECG-Mamba 等 3 篇（2025）可行 | P2，G-D4 参数对齐对照为硬前提 |

### 1.2 三条复盘结论

1. **"往 encoder 上加结构"（注意力/Mixer/GAT/蒸馏/EMA/Sinc/MixUp/HRV）已被内外证据双重关闭**，剩余结构位只有三个：**损失目标值（D1L-fix/D1-ACL）、重建支路（D7）、共源解耦（D3）**——全部作用在损失/目标层，不动 encoder 拓扑，与 E002/E003 负结果不冲突。
2. **"拉近"与"对齐/判别"要严格区分**：LGA（组内拉近、叠加在已有 inter 对齐之上）显著负；ACL（区域内一致 + 区域间判别、替换而非叠加、γ=0.5）显著正。后续一切关系类损失按此二分预审。
3. **数据轴的正确姿势是"单体系外部库"，不是多库乱炖**：N1 的 −3.07pt 是域冲突的预期结果（采样率 257/500/1000 Hz、设备、人群混在一起），与外部经验不矛盾。

---

## 二、Q1 详解：D1L 实现位置错误与修正方案

### 2.1 LFBT inter 损失的真实形式（代码 `run_pt.py:278–295`）

对每对导联 (i,j)，包括 i≠j：

```python
on_diag  = (diag(C^ij) - 1).pow(2).sum()      # 对角目标 = 1（对所有对！）
off_diag = off_diagonal(C^ij).pow(2).sum()    # 非对角目标 = 0
ls = on_diag + λ · off_diag
```

即 **inter 块的对角目标本来就是 1**（与 intra 同形式，仅权重 (1−γ)=0.2 更弱），并非论文文字给人的"跨导全压 0"印象。"去冗余"只发生在非对角项（投影维度间协方差压 0）。

### 2.2 现有 D1L 实现（`run_pt.py:286–290`）

```python
if self.d1l_P is not None and i != j:
    tau = self.d1l_P[i, j]                    # II-III=0.5, 相邻胸导=0.2
    off_diag = (off_diagonal(c) - tau).pow(2).sum()   # ← τ 加在了非对角
```

问题：把非对角目标从 0 改成 0.5/0.2，等于**主动在 2048 个投影维度之间制造交叉协方差**，与 BT 防塌缩/去冗余机制对冲。生理上"II 与 III 相关"指的是**同一投影维度跨导联相关（对角元）**，位置恰好反了。这与观测自洽：AUPRC −0.6 但 AUROC +0.22（结构被改变但未塌缩），且打乱先验更差（NEG −0.96，错误位置上正确先验仍优于随机）。

### 2.3 修正版 D1L-fix（建议实现）

对 inter 块改**对角目标**，非对角保持 0：

```python
if i == j:
    on_diag = (diag(c) - 1.0).pow(2).sum()
else:
    tau = 0.0 if self.d1l_P is None else self.d1l_P[i, j]
    on_diag = (diag(c) - tau).pow(2).sum()   # 差异化跨导对齐目标
off_diag = off_diagonal(c).pow(2).sum()      # 维持 0 不变
```

参数空间因此变得可解释：

- τ≡1（所有 inter 对）≈ 现 LFBT（跨导弱对齐）；
- τ≡0（所有 inter 对）= 论文文字版"跨导完全去相关"；
- τ<sub>II,III</sub>=a、τ<sub>相邻胸导</sub>=b、其余 0 = 结构化关系（D1L-fix 本体）。

**粗探网格**（1 seed，LP+FT10）：a∈{0.25, 0.5, 1.0} × b∈{0.1, 0.25}，共 6 run + NEG（P 矩阵按导联打乱）1 run + τ≡0 对照 1 run；开关关闭逐位一致单测照旧。成本：约 8 个 200ep（fast 车道可减半 epoch 粗探）。
**判据**：最优配置 1-seed ΔAUPRC ≥ +0.5pt（该插槽为机制翻案实验，阈值可低于常规 1pt，但必须 3-seed 同号确认且 NEG 不涨）。

---

## 三、Q2 详解：ACL-ECG（同团队续作）与我们 LGA/S1 的精确差异

Liu, Wu, Yuan（LFBT 原班团队，华东交大），《ACL-ECG: Anatomy-Aware Contrastive Learning for Multi-Lead Electrocardiograms》（预印本，代码未公开）。其结果：PTB-XL LP AUROC/AUPRC = **0.9145/0.7145**（LFBT 0.9082/0.6923），CPSC 0.9394/0.7421，Chapman 0.9960/0.9869；10% 标签追平全监督，5–8× 标签节省。

| 组件 | ACL-ECG | 我们做过的最接近实验 | 差异（=未测部分） |
|---|---|---|---|
| 解剖分组 | 4 组：下壁(II,III)/间隔(V1,V2)/前壁(V3,V4)/侧壁(V5,V6) | LGA/S1：3 组（II,III)/(V1–V4)/(V5,V6) | 胸导 4 合 1 vs 拆 3 组；ACL 消融显示分组粒度敏感 |
| 区域表示 | 组内各导 64 维 **concat 后过独立区域 projector**（2048） | S1/LGA：GAP 特征组内**均值**后直接 BT，无区域 projector | 表示容量与作用位置不同 |
| 区域内损失 | InfoNCE 一致性（两视图） | S1：组均值 BT 对齐（弱正） | BT vs InfoNCE，差异次要 |
| **区域间损失** | **InfoNCE 判别：其他区域同批次特征当负样本，γ=0.5 平衡** | **无（LGA 只有组内项，且 η=0.05 叠加在 LFBT inter 之上）** | **核心缺失臂**：强制区域分工 |
| 增强 | RRC(0.8–1.0) + **CCM 心动周期掩码**（NeuroKit2 检 R 峰，每周期 mask 20%，不跨 R 峰）+ **TS 时移 ±80%** | RRC-TO（裁剪 0.5–1.0 + 随机遮挡 0–50%，可横切 R 波） | CCM、TS 两臂未测；弱裁剪与我们裁剪曲线峰值同向 |
| 编码器 | 同 LFBT（8×VGG α=1/8） | 同 | 公平可比 |

**建议立项 D1-ACL**（D1L 插槽的"外部已验证完整版"，与 D1L-fix 同插槽竞争，按 02 文档 S2′ 规则二选一）：

1. 4 分组常量 + 区域 projector（每组一个 MLP，零结构改动 encoder）；
2. L = L<sub>LFBT</sub> + η<sub>1</sub>L<sub>区域内一致</sub> + η<sub>2</sub>L<sub>区域间判别</sub>，先按 ACL 的 γ=0.5 当量标定，再 val 扫 η；
3. 增强开关化：CCM（NeuroKit2 离线预检 R 峰存缓存，避免训练时开销）+ TS；
4. **NEG 负对照**：随机 4 分组（ACL 自己的消融：随机分组掉 1.0 AUROC/3.5 AUPRC，这是该方向最硬的先验有效性证据）；
5. 与 LGA 的切割写进实验记录：替换式 + 判别臂，而非叠加强对齐。

判据沿用粗筛 1pt/3-seed 门；若 D1-ACL 与 D1L-fix 都为正，跑 S2″（融合显著优于两者才保留）。

---

## 四、Q4：D7 结果回收与失败诊断预案

**先做的事**：核查 A 机 `F:\新实验\ECG_SSL_LFBT-main\checkpoint\M\d7_rec01_seed0`、`d7_rec003_seed0` 是否存在、是否已 LP 评估；09-16 晚队列多次暂停/接力，csv 无行，可能是跑完未评估或中途被切。

现有实现（`run_pt.py:191–215, 308–312`）：每导联独立 decoder 挂在 encoder feat_map 上；**单段连续 50% 时间置零、仅 mask 视图 1**；η∈{0.1, 0.03}。

若回收为负，按以下顺序诊断（每步 1 run，不全面撒网）：

| 嫌疑 | 外部依据 | 改法 |
|---|---|---|
| 单段 50% mask 抹掉整个心拍，任务过难 | MAE 用多随机 patch；ACL-CCM 周期内 20% 不跨 R 峰 | 改多段随机 patch（段长 5–10%×3–5 段）或直接上 CCM |
| η 量级失配 | E007 T5 经验：辅助项末轮占比要核对 | 日志记录 L_rec/L_BT 末轮比值，η 按比值 1–10% 反推，不要拍 0.1 |
| decoder 在 feat_map 上、重建目标在输入空间，映射跨度过大 | MaeFE/MAE-ECG 多在 token/特征空间重建 | 加"特征空间重建"变体（重建未mask视图的同位置特征） |
| 只 mask 一个视图 | 混合目标通行做法两视图独立 mask | 两视图独立掩码 |
| 与 E005 蒸馏的写作划界 | CoRe-ECG（2026，已引 LFBT）已做 12 导对比+重建 | 重建支路服务于关系/解耦主线，独立新颖性不主张 |

D7 与 D1 插槽正交，若转正直接进入 G2 累加（S4 位）。

---

## 五、Q3 详解：数据轴修订（含下载入口与协议设计）

### 5.1 可得性纠正（2026-09-18 核实）

| 库 | 09-15 判定 | 实际 | 入口 | 规格 |
|---|---|---|---|---|
| NFH（宁波一院） | 私有弃用 | **公开**：Challenge-2021 `ningbo` | https://physionet.org/content/challenge-2021/1.0.3/training/ningbo/ | **34,905 条**，12 导 10s 500Hz；PhysioNet 注册+签协议，**免 credentialed 培训** |
| Chapman/Shaoxing | 私有弃用 | **公开** | figshare collection 4560497（CSV 原始+去噪，10,646 人）；WFDB 版见 PhysioNet `ecg-arrhythmia/1.0.0`（绍兴+宁波合集，IRB 明确公开） | 10,646 条，500Hz 10s，11 节律/67 病况 |
| CODE-15% | 未调研 | **公开免认证** | Zenodo 搜 "CODE-15%"（Ribeiro 2021，HDF5+CSV） | **345,372 条**，单体系同设备同流程 |
| MIMIC-IV-ECG | 未调研 | 公开但需培训认证 | https://physionet.org/content/mimic-iv-ecg/ | 800,034 条 + 机器报告；CITI 培训数小时，建议尽早申请 |
| HEEDB | 未调研 | 受控开放（Scientific Data 2026） | Harvard-Emory 申请 | 1160 万条，远期 scaling 用 |

口径提醒：论文写 NFH 43,994 条，公开 ningbo 为 34,905 条（同院同设备，差约 9k 应为未公开版本差异），复现报告中按"公开子集 34,905"口径声明，不与论文数字硬拼。

### 5.2 双锚点协议（解决最大审稿软肋）

当前 B0 用 PTB-XL folds1-8 预训练、folds9/10 下游，虽患者隔离但**同分布**；论文与 ACL 均用外部无标签库预训练。建议：

```text
B0 （保留）：PTB-XL 17,418 预训练        —— 内部九轮结果全部挂此锚点，保证历史可比
B0′（新增）：ningbo 34,905 预训练        —— 论文同源、外部无标签；下游 PTB-XL 官方 strat_fold / CPSC / Chapman
```

- 预处理完全沿用 §04 统一格式（8 导 [II,III,V1–V6]、500Hz、2048 点、逐导 z-score）；ningbo 为 12 导 WFDB，导联映射现成；
- 只需要一次预训练 + 三次 LP/FT，先出 B0′ 的 LP 数字与论文 Table 2/ACL Table 对齐（预期落在论文 0.9082、ACL 0.9145 附近即协议打通）；
- 关键模块（TRC、D1L-fix/D1-ACL 胜者、D7 若正）在 B0′ 上**各补一次确认**，结论从"单库自训"升级为"外部库可泛化"；
- N1 乱炖结论不受影响：ningbo 是单院同设备 35k，域冲突风险低；CODE-15% 是同方向的规模档（345k 单体系），作为 B0′ 之后的可选第二跳，**不要**把 CODE 与小库混合。

### 5.3 修订后的语料轨

| 档位 | 语料 | 用途 | 优先级 |
|---|---|---|---|
| 锚点 B0 | PTB-XL 17k | 内部可比（全部历史） | 已有 |
| 锚点 B0′ | ningbo 35k | 论文/ACL 协议对齐、外部库泛化 | **P0，本周可启动下载** |
| 跨库下游 | +Chapman 10.6k（figshare） | 第三外部测试库（论文同库，数字可直接对照） | P0 随 B0′ |
| 规模档 | CODE-15% 345k | 单体系 scaling 曲线（绕开 N1 域冲突） | P2，视 B0′ 结论 |
| 远期 | MIMIC 800k（认证）/ HEEDB | 不建议本项目阶段投入 | 搁置 |

---

## 六、修订后的推进优先级（对接 M 矩阵纪律）

| 优先级 | 事项 | 与负结果墙的关系 | 粗筛配置 | 成本 |
|---|---|---|---|---|
| **P0** | 回收 D7 结果（A 机 checkpoint 核查） | — | η=0.1/0.03 已跑完或半程 | 1h |
| **P0** | **D1L-fix**：τ 移到对角目标 | 是对 D1L 旧负结果的**实现修正**，非重复 | §2.3 网格 8 run | 低（改 <10 行） |
| **P0** | ningbo 下载入库 + B0′ 锚点 + Chapman 下游 | N1 乱炖已死，本项是单院外部库，不同族 | 1 次预训练+3 下游 | 数据工程 1–2 天 |
| **P1** | **D1-ACL**：4 区域 projector + 组间判别 + CCM/TS + 随机分组 NEG | LGA 只覆盖"组内叠加拉近"，不覆盖本配置 | 先 1 seed 全件，再拆臂消融 | 中 |
| **P1** | D3 共源-特异解耦（吸收已写好的 common_view 雏形） | 未被任何负结果覆盖 | c:s∈{1:1,1:3}，正交 1e-2 | 中 |
| **P1** | D7 若负则按 §四 预案（CCM/patch mask 与 D1-ACL 增强共用实现） | 重建式不受 E005 覆盖 | 见 §四 | 低-中 |
| **P2** | N7 干净 LeadDropout（纯增强，无 mean/FusionBT 混杂）+ D8 物理噪声 | B3 为混合配置，未干净测试；与 TRC 缺导卖点协同 | k∈{1,2}，p∈{0.3,0.5} | 低 |
| **P2** | D4 Mamba（严格 G-D4 同主干参数对齐对照，节律病 vs 形态病分层） | E004 只证轻适配器不成 | 按 02 文档 | 高 |
| **P2** | 多标签官方协议稳健性表（不删多标签、macro AUROC/AUPRC） | 现协议沿用论文/ACL 单标签，主对比不受影响；补表防现代 FM 审稿口径 | PTB-XL 10-fold 口径补跑 LP | 中 |
| 关闭 | GNN/注意力/Mixer 通信、VICReg 族、EMA、患者对、Sinc、MixUp、HRV、多库乱炖、蒸馏族、ECG-文本、backbone scaling | 内外双证或算力/数据不支持 | — | — |

**组合预期（SD-LFBT v2 候选）**：TRC（已立）×（D1L-fix 或 D1-ACL 胜者）× D7（若转正）× 弱裁剪 0.8 + CCM，在 B0/B0′ 双锚点各出一版；鲁棒章由 TRC + N7 + 37 条件管线承担。

---

## 七、成文建议（外部定位）

1. **LRTC-Net 应用论文**（JBHI/BSPC/CBM 档，已成稿体系）：主张句建议为"a 1,024-parameter temporal adapter for label-efficient and lead-missing-robust multi-lead ECG SSL"；外部引用包装：缺导鲁棒引 TolerantECG（ACM MM 2025）、标签效率引 ACL-ECG（5–8×）、混合目标趋势引 ECG-FM；补多标签 macro 稳健性附表。
2. **SD-LFBT 工程报告**：九轮负结果 + 3-seed 符号门 + 逐位一致纪律是差异化资产（外部多数 ECG-SSL 论文不报告失败探针），motivation 对照表保留并扩充（VICReg/EMA/患者对/Sinc/MixUp/HRV 为本轮新增负结果）。
3. **D1L 翻案若成立**，故事线为"结构化跨导目标的正确实现位置"，配合 NEG 与 ACL 外部对照，可作为工程报告中损失章的主结果；若 D1-ACL 复现成功，则两篇共用一套区域消融，互相加强。
4. 与 ACL-ECG（同团队、未正式见刊/代码未放）的关系需在文中明确引用并划界：我们用 BT 族无负样本框架 + 目标值修正，ACL 用 InfoNCE 判别，二者是同插槽两条技术路线。

---

## 八、参考入口

- ACL-ECG 全文：https://pdfs.semanticscholar.org/6f12/4cbe1dc827181be0f7fc428470f88cde7916.pdf
- LFBT 官方仓库（NFH 链接即 Challenge-2021 ningbo）：https://github.com/Aiwiscal/ECG_SSL_LFBT
- Challenge-2021（含 ningbo 34,905）：https://physionet.org/content/challenge-2021/1.0.3/
- Chapman/Shaoxing figshare：https://figshare.com/collections/ChapmanECG/4560497/2 ；PhysioNet WFDB 版：https://physionet.org/content/ecg-arrhythmia/1.0.0/
- CODE-15%：Zenodo 检索 "CODE-15%"（Ribeiro et al. 2021）
- MIMIC-IV-ECG：https://physionet.org/content/mimic-iv-ecg/
- ECG-FM（混合目标+开源权重+benchmark）：https://github.com/bowang-lab/ECG-FM
- CoRe-ECG（12 导对比+重建，已引 LFBT，划界用）：https://arxiv.org/html/2604.11359v1
- TolerantECG（任意导量子集，ACM MM 2025）：https://arxiv.org/pdf/2507.09887
- LVCG（VCG 潜空间 SSL，D3 的远亲参照）：https://arxiv.org/pdf/2605.31249v1
- MAE-ECG（Sawano 2024，D7 mask 策略参照）：https://pmc.ncbi.nlm.nih.gov/articles/PMC11324121/
