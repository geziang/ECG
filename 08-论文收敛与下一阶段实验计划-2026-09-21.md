# ECG SSL 论文收敛与下一阶段实验计划

**版本**：2026-09-21（基于 Git `b17cb44`）  
**仓库**：<https://github.com/geziang/ECG>  
**适用对象**：主机 A（RTX 4090 24 GB）、主机 B（RTX 3080 10 GB）  
**文档性质**：教授级实验审查、论文主线决策、下一周代码与实验任务书

---

## 0. 先给结论

本周没有得到“再加一个模块就超过 B0”的证据，但已经得到一条更有研究价值、可以收敛成论文的证据链：**同域预训练、外部医院预训练和轻量时序重标定的收益取决于数据血缘与下游标签规模；简单结构化导联关系目标在当前 LFBT 协议下没有迁移成功。**

下一周不再继续扫描注意力、Mixer、导联硬拉近和新的随机模块。主线收敛为：

1. **主线 C：NFH 外部预训练 + TRC/GRN1D 的跨域和少标签确认。** 当前 C2 在 CPSC 上 `0.8092`，接近同域 B0 的 `0.8105`，但只有 seed 0，必须完成 paired 3-seed 才能写成结论。
2. **辅助线 B：CCM 周期遮挡重建。** 当前只有代码和队列，没有 B1/B2/B3 结果落账。只做最小受控比较，若 B3 不能在 val 或 FT10 上显示机制优势，立即关闭。
3. **失败分析 A：D1L/ACL 作为边界条件。** A 线已经足够关闭，不能再写成“ACL-ECG 机制被证伪”，只能写成“ACL 风格区域目标在当前 LFBT + PTB/NFH 协议下未迁移”。

论文有两个合法落点，下一周周末按结果二选一：

- **方法论文落点**：C2 在 CPSC、FT10 或缺导场景上 3-seed 稳定优于 C1，且增益不依赖 split 偶然性。题目可围绕“轻量时序重标定的跨域多导联 ECG SSL”。
- **协议/实证论文落点**：C2 不稳定，但 C1/B0/C2 的血缘效应、标签效率、缺导相对鲁棒性和系统失败分析稳定复现。题目可围绕“数据血缘和协议对多导联 ECG SSL 迁移的影响”。不能为了保住方法故事而挑选单次正结果。

## 0.1 实验状态总表（更新时间：2026-09-21）

状态含义：**已完成**=结果已入库且可复核；**已完成/单 seed**=本轮已跑完，但不能作为最终统计结论；**未完成**=只有代码、设计或排队记录；**已关闭**=按止损规则停止，不再追加同类扫描。

| 实验/交付物 | 当前状态 | 已有证据 | 未完成部分与明确时间 |
|---|---|---|---|
| B0 本机锚点与基线审计 | **已完成** | 主机 A `0.7177`、主机 B `0.7158`，冻结清单和 checkpoint 哈希已入库 | 无；后续候选必须使用同机 paired delta |
| A1 D1L-fix | **已完成/已关闭** | 2026-09-21，AUPRC `0.6471`，相对 B0 `-7.06 pt` | 不再追加 D1L 超参；只保留失败分析 |
| A2 ACL intra、A3 ACL full/inter | **已完成/已关闭** | 2026-09-21，`0.6266`、`0.6994`；a3 比 a2 回升 `7.30 pt` | 不再追加 ACL 变体；不宣称严格复现 ACL-ECG |
| C1 NFH-only seed0 | **已完成/单 seed** | NFH manifest、matched updates、PTB/CPSC/Chapman LP、PTB FT10 已落账 | 作为 C2 paired 对照补 seed2/4：**9月23-24日** |
| C1 缺导 37 条件 | **已完成/单 seed** | `missing_lead_curves.csv` 74 行；相对掉幅更小但无绝对反超 | C2 缺导关键条件：**9月25日** |
| C2 NFH+TRC 预训练 seed0 | **预训练已完成；评估未完成** | checkpoint 和训练日志已落账 | 评估账本冲突，LP/FT10 需重跑：**9月22日修脚本，9月23-25日重评** |
| C2 跨库 LP/FT10 | **未完成** | 现有数字来自 `run_lp.py`，但 CSV 表头混入 FT10，标签未冻结 | 生成独立 `c2_lp.csv`、`c2_ft10.csv`，seed0/2/4：**9月23-25日** |
| `run_ft.py` 的 TRC 架构传递 | **未完成** | `run_lp.py` 支持 `--trc`，FT 链路未闭合 | 修复、smoke、checkpoint 校验：**9月22日** |
| B1/B2/B3 D7/CCM | **未完成** | `ccm.py`、R 峰缓存和 decoder 已实现；队列未产生结果 | 5 epoch smoke：**9月24日**；单 seed full：**9月25-26日** |
| val-only 筛选、断点续训、统一 metadata | **未完成** | 部分字段已有，W1 A 线仍用遗留 test 粗筛 | 代码闸门与回归检查：**9月22日** |
| 论文主表、统计区间和失败表 | **未完成** | 原始日志、冻结清单和失败结果已在仓库 | 冻结 test、paired delta、bootstrap/permutation：**9月26-27日** |

### 下一周固定日程

| 日期 | 主机 A | 主机 B | 当日必须交付 |
|---|---|---|---|
| **9月22日 周二** | 修复 C2/FT 评估脚本并做 smoke | 回归 B0、加载 TRC checkpoint、验证 metadata | 代码 SHA、评估表头分离、val-only 闸门 |
| **9月23日 周三** | C1/C2 seed2 预训练或 LP/FT | 并行完成另一配置 seed2 下游评估 | C1/C2 seed2 的 LP 和 FT10 原始 JSON |
| **9月24日 周四** | C1/C2 seed4 预训练或下游 | B1/B2/B3 5 epoch smoke | seed4 结果；R 峰/fallback/梯度诊断 |
| **9月25日 周五** | C1/C2 三 seed 跨库 LP、FT10 | C2 关键缺导；B 线优胜者 full | 配对 delta 表和缺导 CSV |
| **9月26日 周六** | 最终候选冻结后只评一次 test | 统计复核、per-class AP、混淆矩阵 | checkpoint/数据/代码哈希冻结 |
| **9月27日 周日** | 论文主表和图 | 失败分析与限制章节 | 可直接写入论文的结果包 |

---

## 1. 当前仓库状态与证据等级

仓库当前 `HEAD=b17cb44`，与 `origin/main` 一致，工作区干净。主机 A 的 W1 报告、冻结清单、NFH manifest、跨库表和缺导曲线已入库。主机 B 的 W1 队列仍列有 B1/B2/B3 和随机 ACL，但没有新的结果写入 `matrix_results_hostB.csv`，因此 B 线只能标记为“实现完成、运行结果待回收”。

### 1.1 B0 锚点

主机 A 的 PTB-XL B0 LP AUPRC 为 `0.7177`；主机 B 的本机 B0 为 `0.7158`。两台机器的绝对分数不能直接相减，后续所有候选必须使用**同机、同 seed、同下游 split 的 paired delta**。原始 LFBT 论文使用 NFH/SNPH 外部预训练，PTB-XL LP AUPRC 报告为 `0.6923`；本仓库的 PTB folds 1–8 预训练 B0 不是论文的逐协议复现，只能作为本机基线。

### 1.2 A 线：已关闭，但报告需要降级措辞

| 实验 | AUPRC | 相对主机 A B0 | 证据等级 |
|---|---:|---:|---|
| B0 | 0.7177 | 0 | 锚点 |
| D1L-fix，`τ=(0.5,0.2)` | 0.6471 | -7.06 pt | 单 seed，明确负 |
| ACL intra | 0.6266 | -9.11 pt | 单 seed，明确负 |
| ACL intra+inter | 0.6994 | -1.83 pt | 单 seed，明确负 |

D1L 修复了旧实现把目标值放在 off-diagonal 的错误，但修复后仍然大幅退化。这个结果足以关闭**当前实现和当前协议**，不能扩大成“任何生理拓扑目标都有害”。ACL 的跨区域项相对 intra-only 回升 `7.30 pt`，说明 Eq. (11) 的正对设计有机制作用；但完整配置仍低于 B0，且当前 `run_pt.py` 是 `L_LFBT + L_ACL` 的叠加，并非报告中所称的严格替换。因此正确写法是：

> 在 batch=128、当前 projector、PTB-XL 同域预训练和 ACL 辅助损失尺度下，区域关系目标未能迁移到 LFBT；跨区域正对能缓解区域内目标的损失，但不足以恢复 B0。

不要写“结构化导联关系目标真实有害”或“ACL-ECG 被证伪”。ACL 论文使用 batch=64、NFH 外部预训练、不同损失归一化和不同模型协议；本实验没有完成严格复现。

### 1.3 C 线：目前最有价值，但仍是趋势

| 预训练 | PTB-XL LP | CPSC LP | Chapman LP | PTB FT10 |
|---|---:|---:|---:|---:|
| B0，PTB 同域 | 0.7177 | 0.8105 | 0.9796 | 0.6433 |
| C1，NFH 外部 | 0.6574 | 0.7857 | 0.9891 | 0.6341 |
| C2，NFH+TRC | 需重评 | 需重评 | 需重评 | 需重评 |

**账本阻断项**：`runlog/W1/c3_evals.csv` 在 C2 行之前已经写入了 `ft10_auroc,ft10_auprc` 表头，但 `runlog/W1/c2_chain.py` 的 `lp_eval()` 实际调用的是 `run_lp.py`。因此当前 C2 的 `0.8860/0.6564`、`0.9586/0.8092`、`0.9966/0.9889` 不能直接放入论文 LP 或 FT10 主表。下一周必须重新运行并分别生成 `c2_lp.csv`、`c2_ft10.csv`，同时修正写表脚本；在修正完成前，C2 只能描述为“未冻结的单种子跨库读数”。

C1 相对 B0 在 PTB-XL 下降 `6.03 pt`，在 CPSC 下降 `2.48 pt`，但在 Chapman 上提高 `0.95 pt`。这不是一个简单的“外部预训练更好/更差”的结论，而是**数据血缘效应的模式**：NFH 与 Chapman 存在同团队/同设备发布血缘，Chapman 的优势不能直接解释为医院外部泛化；CPSC 是更有区分力的第三域。C2 在 CPSC 比 C1 提升 `2.35 pt`，并接近 B0，但目前只有一个 seed。

### 1.4 缺导结果

在 37 个导联置零条件下，C1 没有任何条件绝对超过 B0，但平均单导下降为 `-0.60 pt`，B0 为 `-0.83 pt`；双导平均下降分别约 `-1.47 pt` 和 `-1.88 pt`。这支持“外部预训练表征相对更稳”的假设，不支持“外部预训练绝对性能更高”。II+III 同时缺失最敏感，V5/V6 缺失较小。这里是**导联置零**，不是物理电极错位，论文必须使用准确术语。

### 1.5 主机 B 已有的负结果

主机 B 已入库的 n3 患者正对、Sinc 前端、H3 去相关、两种 MixUp 和 HRV 辅助均为负，AUPRC 变化约为 `-0.97`、`-2.71`、`-14.98`、`-1.69`、`-1.44`、`-0.26 pt`。这些结果可以关闭当前实现和超参区间，不能宣称整个方法族理论上失败。

---

## 2. 对“为什么我们做不出来、别人做出来”的教授级判断

### 2.1 第一根因：复制了模块名称，没有复制完整实验协议

原 LFBT 论文的关键不是“用了 Barlow Twins”这一句，而是 8 个独立导联 encoder、MBC、intra/inter 融合损失、NFH/SNPH 预训练、下游 LP/FT 和特定数据划分共同形成的协议。我们早期实验大多在 PTB-XL folds 1–8 同域预训练上改一个小模块，再用单次下游分数判断成败。别人论文里的正结果来自完整协议，不能把其中一个模块单独移植后期待同样的收益。

### 2.2 第二根因：增强和目标改变了表征要解决的问题

LFBT 的 inter-lead BT 是去冗余目标：不同导联 projector 的不同坐标不应被无条件拉到相同值。LGA 把同组导联硬拉近，直接抹掉了导联特异性；D9 又把不同导联原始投影逐维 MSE 对齐，任务定义不对应原论文。D1L 虽然修正了 τ 的位置，但当前 `LFBT + ACL` 叠加会改变原始损失尺度。负结果首先说明**目标函数定义不适配当前表征空间**，不是简单说明“加模块没有用”。

### 2.3 第三根因：强基线附近，单次正结果小于实验噪声

B0 的跨 seed 变化约为 0.8 个百分点量级，而许多历史候选只有 0.2–0.4 pt 的单次提升。若只看一个 seed，任何“正收益库”都会混入随机性。下一轮必须采用 val 选择、同机 paired B0、3 seeds 和冻结后单次 test；3/3 同号只能作为复现趋势，不能直接写成统计显著。

### 2.4 第四根因：筛选时复用了 test，导致选择偏差

W1 A 线沿用了遗留 test 粗筛管道。由于候选全部明显为负，关线决定没有被扭曲，但这种协议不能继续使用。任何候选晋级都只能看 val；test 只在代码、超参和 checkpoint 冻结后评一次。

### 2.5 第五根因：数据血缘和泄漏审计没有成为主问题

NFH manifest 没有患者 ID，只能以记录内容 SHA 做重复审计；主机 A 保留 34,905 条，主机 B 因 non-finite 排除 97 条。CPSC/Chapman 当前是 record-level stratified 70/10/20 split，不是官方 patient split；NFH、Chapman 的编号区间和采集设备血缘也必须在论文中说明。若不把这些因素写清楚，任何跨域分数都可能被误读为模型创新。

---

## 3. 下一周唯一主线：C2/TRC 的三种子闭环

TRC 是 block5+GAP 前的零初始化 GRN1D，每导联 128 个参数，8 导联共 1024 个参数。它的价值不是“大模块创新”，而是一个可检验的假设：**外部预训练产生的时序表征在目标域统计变化下需要极小的通道响应重标定；该重标定对第三域 CPSC 和少标签 FT 更有帮助，而不应普遍改变 PTB/Chapman。**

### 3.1 必跑矩阵（未完成项）

每个配置跑 seed `0,2,4`，并在同一主机使用相同 downstream seed：

| 配置 | 预训练 | 下游 | 必要性 |
|---|---|---|---|
| C1 seed0 | NFH-only，`--trc 0` | PTB LP、CPSC LP、Chapman LP、PTB FT10 | **已完成/单 seed** |
| C1 seed2/4 | NFH-only，`--trc 0` | 同上 | **未完成，9月23-24日** |
| C2 seed0 | NFH-only，`--trc 1` | 当前跨库读数已生成但账本未冻结 | **已完成/单 seed，9月22日修账本** |
| C2 seed2/4 | NFH-only，`--trc 1` | 同上 | **未完成，9月23-25日** |
| B0-local | PTB folds 1–8，`--trc 0` | PTB LP、FT10 | **已完成锚点；补跑仅在机器/协议变化时需要** |

如果时间不足，优先级为：C2/C1 的 CPSC LP → PTB FT10 → 缺导 → Chapman；不能只重复 Chapman，因为它可能受数据血缘影响。

### 3.2 代码先决条件

1. `run_ft.py` 和 `models/mbn.py` 当前没有完整的 `--trc` 传递链。先为 FT 增加 `--trc`、`--blur-pool`、`--pool-power` 等与预训练一致的 architecture args，并在 checkpoint config 中自动校验；否则 C2 FT10 不能作为可审计结果。
2. 修正 `runlog/W1/c1_chain.ps1`、`c2_chain.py` 的硬编码 `F:\新实验` 和 `C:\Users\admin` 路径，改为仓库根目录相对路径或环境变量。
3. 每个 run 必须写入 `git_sha/host/gpu/torch/seed/dataset_hash/preprocess_version/batch/epochs/val/test/checkpoint_sha256`。训练中断后支持断点续训，避免重启事故造成不可复核的半程结果。

### 3.3 推荐命令模板

在 `ECG_SSL_LFBT-main` 根目录执行；`$PY` 替换为各机器实际 CUDA 环境：

```powershell
$PY = "<本机 python>"
$S = 0   # 依次改为 0、2、4

# C1: NFH 外部预训练
& $PY -u run_pt.py --data-dir data/pt_pretrain_nfh --epochs 100 `
  --batch-size 128 --workers 4 --seed $S --trc 0 `
  --checkpoint-dir checkpoint/confirm/c1_seed$S

# C2: NFH + TRC
& $PY -u run_pt.py --data-dir data/pt_pretrain_nfh --epochs 100 `
  --batch-size 128 --workers 4 --seed $S --trc 1 `
  --checkpoint-dir checkpoint/confirm/c2_seed$S

# CPSC LP（C2 示例）
& $PY -u run_lp.py --data-dir data/cpsc --num-classes 9 `
  --checkpoint checkpoint/confirm/c2_seed$S/encoder_group.pth `
  --feat-dir results/confirm/c2_cpsc_seed$S --seed $S --workers 6 --trc 1
```

FT10/FT100 只有在 `run_ft.py` 完成 architecture args 传递后运行。每个 seed 记录 `C2-C1` 和 `C2-B0-local`，不要把 `0.8092-0.8105` 直接称为显著提升。

### 3.4 C2 的晋级和止损规则

- CPSC LP：3 个 seed 中至少 3/3 同号，均值相对 C1 不低于 `+0.5 pt`，才保留为主结果。
- PTB FT10：至少 2/3 同号且均值不低于 `+0.5 pt`，才支持标签效率故事。
- 如果 CPSC 仅 1/3 或 2/3 正，或者增益随 split 改变，C2 降为趋势分析，不再叠加新模块。
- 如果 C2 clean 下降但缺导相对掉幅稳定更小，论文可保留为“鲁棒性适配器”，不能写成普遍性能提升。

---

## 4. B 线：只做一次有机制的 CCM 重建判定

B 线不是当前主论文的必需创新，必须服从“能否区分生理相位 mask 与遮挡强度”的问题。当前 `data_utils/ccm.py`、R 峰缓存和 masked MSE 已实现；结果尚未进入仓库。

### 4.1 最小矩阵（全部尚未完成结果回收）

| 配置 | 作用 | 运行 |
|---|---|---|
| B1 | 旧 D7，单段 50%，仅 view1，归档审计 | `--rec-style d7 --d7-weight 0.1`；**9月25日** |
| B2 | 多片段总遮挡 20%，两视图独立 | `--rec-style multiseg --rec-ratio 0.2 --d7-weight 0.1`；**9月25日** |
| B3 | 每个 RR 周期内遮挡 20%，不跨 R 峰 | `--rec-style ccm --rec-ratio 0.2 --d7-weight 0.1`；**9月25-26日** |

先做 5 epoch smoke，再做单 seed full。必须记录 R 峰成功率、CCM fallback 比例、`L_rec/L_BT`、`grad_rec_bt`、val LP 和 FT10。若 fallback 高于 15%、重建梯度长期超过 BT 的 20%–30%，或 val 比 B0 低超过 0.5–1.0 pt，立即停止。B3 只有在优于 B2 且改善集中于 FT10/缺导时才进入 3-seed；否则关闭波形重建线，不再增加 decoder。

### 4.2 B 线不能重复的错误

重建目标是同一视图的原波形，不能引入 teacher 或跨导联逐维 raw MSE；两视图 mask 必须独立；mask 同步跨导联只表示同一时间缺口，不等于强制不同导联表征相同。这个边界是为了避免重演 E005 蒸馏和 E007 硬拉近的失败。

---

## 5. 下一周按天执行

### 周一：协议与代码闸门（9月21日，已完成的 W1 基础工作）

- NFH manifest、B0 锚点、A 线关闭和 C1 seed0 已完成并入库。
- 以下仍需在 **9月22日**完成：补 `run_ft.py` 的 TRC 参数、run metadata、val-only 选择、断点续训。
- 两机 B0 smoke、checkpoint load regression 和 NFH A/B manifest 敏感性口径核对也安排在 **9月22日**。

### 周二：评估账本修复与 smoke（9月22日）

- A 机修复 C2/FT 评估链并做 smoke；B 机回归 B0、加载 TRC checkpoint、验证 metadata。
- 只读 val 决定是否继续，不查看最终 test 晋级候选。

### 周三：seed2 与下游评估（9月23日）

- A 机完成 C1/C2 seed2 预训练或下游评估；B 机并行完成另一配置 seed2。
- 交付 C1/C2 seed2 的 LP、FT10 原始 JSON，仍只看 val 选择。

### 周四：seed4 与 B 线 smoke（9月24日）

- A 机完成 C1/C2 seed4 的预训练或下游评估；B 机执行 B1/B2/B3 5 epoch smoke。
- 检查 R 峰成功率、fallback、`L_rec/L_BT` 和梯度比；若架构参数仍未闭环，禁止写入 C2 FT10 主表。

### 周五：三 seed 确认与跨域（9月25日）

- 完成 C1/C2 三 seed 的 CPSC、PTB、Chapman LP 和 FT10；B 机完成 C2 关键缺导与 B 线优胜者 full。
- 生成 paired delta 表和缺导 CSV；只有通过 val 门槛的配置才进入 test。

### 周六：冻结前评估与统计（9月26日）

- 最终候选冻结后，每个 checkpoint 只评一次 test；保存 per-class AP、AUROC、AUPRC、混淆矩阵和 SHA。
- 生成 seed 符号表、均值±标准差、patient-level bootstrap 或 paired permutation，并审查数据血缘、split、预训练/测试重叠和机器差异。

### 周日：论文材料冻结（9月27日）

- 输出主结果表、消融表、失败方向表、缺导热图、C1/C2 数据流图和协议差异说明。
- 根据 C2 是否通过门槛，在“方法论文”与“协议/实证论文”两个落点中选一个，不再同时包装两个故事。

---

## 6. 论文写作结构与必须补齐的表

### 6.1 推荐章节

1. **Introduction**：多导联 ECG SSL 的两个现实问题——外部数据血缘和少标签/缺导鲁棒性。
2. **Baseline and Protocol**：LFBT、8 导联、预训练隔离、NFH manifest、matched optimizer updates。
3. **Method**：TRC 的插入位置、零初始化、参数量、为什么只做时序重标定不改导联关系目标。
4. **Main Results**：B0/C1/C2 的 PTB-XL、CPSC、Chapman LP；FT10/FT100。
5. **Cross-domain Analysis**：C1 在 PTB/CPSC/Chapman 的不同表现，解释数据血缘而非简单平均。
6. **Robustness**：缺导联置零、关键双导组合，必要时加入噪声 SNR 曲线。
7. **Ablation**：TRC off/on、参数量、插入位置、训练开销、C1/C2 matched updates。
8. **Failure and Boundary Analysis**：D1L、ACL、LGA、蒸馏、MixUp、Sinc、HRV、CCM（若负）。
9. **Limitations**：单卡、3 seeds、record-level split、NFH 缺患者 ID、8 导联而非 12 导联。

### 6.2 论文不能出现的表述

- “C2 在 CPSC 提升 2.35 pt”——在 3-seed 前只能写“seed 0 趋势”。
- “外部预训练提高缺导绝对性能”——当前没有任何缺导条件反超 B0。
- “ACL-ECG 被我们复现并证伪”——协议和损失实现不完全相同。
- “跨医院泛化”——当前 CPSC/Chapman 是 record-level split，且 Chapman 与 NFH 有血缘疑虑。
- “电极错位”——当前缺导实验是置零；真正的 lead swap 是另一条评估。

---

## 7. 参考文献与它们对本项目的实际启示

1. Liu et al., **Lead-fusion Barlow twins**, *Information Fusion* 114 (2025) 102698，DOI `10.1016/j.inffus.2024.102698`。启示：LFBT 的收益来自导联独立 encoder、MBC、intra/inter BT 和外部 NFH/SNPH 协议的组合，不能只移植损失名。
2. Liu, Wu, Yuan, **ACL-ECG: Anatomy-Aware Contrastive Learning for Multi-Lead Electrocardiograms**, *Sensors* 26 (2026) 1080，DOI `10.3390/s26031080`。启示：四区 concat、独立 projector、intra/inter 正对是完整机制；本仓库 A 线只可作为迁移边界分析。
3. Kiyasseh et al., **CLOCS**, ICML 2021，arXiv `2005.13249`。启示：患者/时间层级正对必须有严格身份和时间定义，简单把记录换成同患者正对会改变任务语义。
4. McKeen et al., **ECG-FM**, arXiv `2408.05178`。启示：大规模、多来源预训练的收益与语料规模和血缘有关，不能用单一医院或单一下游分数概括。
5. Na et al., **ST-MEM**, arXiv `2402.09450`；Song et al., **CREMA**, arXiv `2407.07110`；Qin et al., **CoRe-ECG**, arXiv `2604.11359`。启示：重建/掩码任务需要控制 mask 语义、重建损失尺度和 decoder 投机，B 线只能作为可诊断的辅助线。

---

## 8. 最终验收清单

- [ ] C1/C2 seeds 0/2/4 的预训练、checkpoint SHA 和 optimizer updates 齐全。
- [ ] C2 的 LP/FT 使用与预训练一致的 TRC architecture，`run_ft.py` 已有参数校验。
- [ ] 每台机器都有本机 B0 anchor，所有 delta 是 paired delta。
- [ ] 候选由 val 选择，test 只在冻结后评一次。
- [ ] CPSC/Chapman split 和数据血缘在主文和 limitation 中明确。
- [ ] NFH A/B 清洗差异、重复记录和无患者 ID 局限已记录。
- [ ] C2 是否晋级由 3-seed 预注册门槛决定，而不是由单次 test 分数决定。
- [ ] B1/B2/B3 若没有结果，论文写“未验证”；不能写“失败”或“完成”。
- [ ] 所有历史负结果保留原始日志、配置和 checkpoint，不删除、不改写。

**本文件的核心判断**：下一周的目标不是再寻找一个偶然高分，而是把“外部数据血缘 → 轻量时序适配 → 跨域/少标签/缺导评估”的因果链闭合。若链条闭合，论文有一条清晰的方法主线；若链条不闭合，严谨的协议和失败分析本身就是可投稿的实证结果。
